# Violet Homey App — Versions-Log (App-Version ↔ GitHub-Commit)

Nachvollziehbarkeit: **jede** auf das Homey installierte (`homey app install`) **oder** in den
Store veröffentlichte App-Version trägt eine eindeutige Versionsnummer und ist hier einem exakten
GitHub-Commit zugeordnet. So lässt sich jederzeit feststellen, welcher Code-Stand auf der Hardware
bzw. im Store läuft.

Versionsschema, Release-Ablauf und die Homey-CLI-Befehle stehen in
[`CLAUDE.md` §8](../../CLAUDE.md) (generisch) und [`HOMEY.md`](../../HOMEY.md) (Homey-spezifisch).
Diese Datei ist die reine Log-Tabelle für dieses Projekt.

## Log

| Version | Datum | Commit | Ziel | Milestone | Notiz |
|---|---|---|---|---|---|
| `0.1.0` | 2026-06-24 … 06-29 | `4d1aaf3` | Homey-Install | M0 | Scaffold-Default (Homey `app create`), **nie hochgezählt** — alle M0-Installs (`c174267` → clear-on-stale `4d1aaf3`, aktuell live) liefen unter 0.1.0. Grandfathered; Auslöser für diese Konvention. |
| `0.1.1` | 2026-06-30 | `0740c5f` | origin/main (Homey-Install) | M1 | LSI-Flaggschiff (optional via Toggle, Bänder nach ANSI/PHTA/ICC-11, CYA pH-abhängig) + Freshness-Refactor auf PUMP_LAST_ON. 12 SDD-Tasks, alle reviewt; Whole-Branch-Review (opus): „ready to merge". |
| `0.1.2` | 2026-07-01 | `67812c6` | origin/main + Homey-Install | M1 | Kosmetik-Patch: feste LSI-Insights-Skala (`measure_lsi` min/max −3..+3, Achsen-Experiment gegen milli-Beschriftung) + iOS-Dropdown-Hinweis im LSI-Info-Label. |
| `0.1.3` | 2026-07-01 | `0ac021f` | origin/main + Homey-Install | M1 | Neu: `alarm_water_balance` (Kachel-Alarm, true wenn LSI außerhalb −0,3…+0,5; Option A) — sichtbar auf der Kachel wie ein Bewegungsalarm. Totes `measure_lsi` min/max entfernt (Homey nutzt es nicht für die Insights-Achse). |
| `0.2.0` | 2026-07-01 | `b4c0d2a` | Homey-Install (→ origin/main) | M2 | Volle Reads + Feature-Gruppen: 27 neue Capabilities (Pumpenstufe, Heizung/Solar, Abdeckung als Enum, Licht, Wasserstand, Rückspülung, Dosier-Verbrauch je Kanal, Diagnose) hardware-adaptiv erkannt + 5 Alarm-Kacheln/Flow-Trigger (Dosierung blockiert/niedrig, Überlauf trocken/über, Ventilfehler). 10 SDD-Tasks + Whole-Branch-Review (opus). Live verifiziert (Detektion adaptiv, `alarm_dosing_low.cl`=true bei 6 d, Diagnose ausgeblendet). |
| `0.3.0` | 2026-07-04 | `727dfc5` | Homey-Install | M3 | Schreib-/Steuerzugriff: Pumpe/Licht/DMX-Szenen/PV-Überschuss via `setFunctionManually`+BasicAuth (Manual §26.2–26.3), als settbare Kacheln + 5 Flow-Aktionen. Interlock `control_enabled` (Default AUS) gated jeden Write; Args gegen `WRITE_TARGETS` geklemmt/abgelehnt; Credentials nur im Device-Store, nie in URL/Log. 9 SDD-Tasks (frischer Implementer+Reviewer je Task) + security-reviewer (SR-01..10 PASS) + Whole-Branch-Review (opus) + SR-10-Audit-Fix. |

| `0.3.1` | 2026-07-05 | `074d486` | Homey-Install | M3 | Diagnose-Hilfe (Live-Fehlersuche): bei aktivierter „Show Advanced diagnostics" hängen Status-/Alarm-/Schalt-Kacheln UND deren Homey-Logbuch-/Verlauf-Einträge hinter einem „:" den exakten getReadings-Rohwert an (z. B. `alarm_dosing_blocked.cl` → `[CL_DOSING_CONTROLLER]`, `cover_state` → `OPEN`). Reine lib-Mapping (`diagAnnotatable`/`diagRawValue`, 2 Unit-Tests, 62/62) + device.js `setCapabilityOptions` nur bei Wertänderung, Revert wenn aus. Live-Messwerte bewusst ausgenommen (Churn). |

| `0.4.0` | 2026-07-07 | `198ac4b` | **Homey App Store (privater Test)** | M4 | **Erster Store-Publish** (privater Test, `homey app publish`). Publish-Readiness: `validate --level=publish` PASS; Store-Name „Violet + BADU Blue Pool Control" (BADU Blue baugleich), Hersteller-Assets (App-Icon font-frei geflattet + zentriert, App-Bilder 250×175/500×350/1000×700, Driver-Bilder 75×75/500×500/1000×1000), Driver-Icon (stilisiertes VIOLET-Modul), explizite `images`-Manifeste, App-ID `de.neunbft.violet` final. mDNS live gedroppt (Violet kündigt keinen SD-Dienst an) → manueller `violet.local`-Fallback, Athom-dokumentiert. 8 SDD-Tasks + release-readiness + /code-review. |
| `0.4.1` | 2026-07-07 | `e0b8dcd` | **Homey App Store (privater Test)** | M4 | Store-Politur nach erstem Test: Titel `&`/`and` → `+` (beide Sprachen), README en+de auf Absätze in Einzelzeilen umgestellt (Store hatte harte Umbrüche gerendert), App-Icon als reines Vektor-SVG neu aufgebaut. **Icon aber weiterhin weißes Quadrat** — Ursache war NICHT `href` (das war ein Nebenaspekt), sondern die Maskierung (siehe 0.4.2). |
| `0.4.2` | 2026-07-07 | `4cd56da` | **Homey App Store (privater Test)** | M4 | **Icon-Fix (Kernursache):** Homey rendert App-/Driver-Icons als **einfarbige Maske** (Farben egal, nur die Form wird weiß gefüllt) → ein vollflächiger Hintergrund wurde zum weißen Vollquadrat (im Store UND auf der Geräte-Kachel). Beide Icons neu als **transparente, einfarbige Strichgrafik**: App = weißes U + Trennlinie + UIOLET; Driver = weißer Modul-Umriss mit Klemmen-Pins, LED-Reihe, Akzentlinie. |
| `0.4.3` | 2026-07-10 | `cd88db4` | Homey-Install | M5 | Wartungs-Release: strict-Ratchet-Typsicherheit (M4.5–M5, typing-only JSDoc + verhaltensgleiche Refactors `?? NaN`/Feld-Deklarationen/`async onPair`) — **keine Funktionsänderung** ggü. 0.4.2. Bump nur für monoton steigende Version. Nicht in den Store publiziert (`homey app publish` durch release-gate (c) blockiert, bis Write-Passwort rotiert + `credential-rotation.md` mit Datum existiert). Live auf „Torstens Homey Pro" verifiziert (validate PASS, Archiv 1.31 MB/34 Dateien). |
| `0.4.4` | 2026-07-10 | `3d0d94c` | **Homey App Store (privater Test)** + Homey-Install | M5 | Code-gleich zu 0.4.3 (Wartungs-Release, keine Funktionsänderung). Eigene Nummer, weil jedes Release-Event eine eindeutige Version bekommt (0.4.3 war schon als Homey-Install geloggt → release-gate Doppel-Release-Schutz). Write-Passwort **rotiert** (2026-07-10, `docs/superpowers/security/credential-rotation.md`) → release-gate (c) erfüllt, `homey app publish` freigegeben. Als privater Test publiziert + auf „Torstens Homey Pro" installiert. |
| `0.4.5` | 2026-07-11 | `f19ea0f` | **Homey App Store (öffentlich — Zertifizierung eingereicht, Freigabe ausstehend)** | M5.5 | Go-Live-Release: Code-gleich zu 0.4.4 (keine Funktionsänderung), Manifest um `homeyCommunityTopicId` (157109, Support-Thread https://community.homey.app/t/violet-badu-blue-pool-control-for-homey/157109) ergänzt. Repo öffentlich (README/LICENSE finalisiert). Bilingualer Changelog, `validate --level publish` PASS. Nutzer hat `homey app publish` selbst ausgeführt und öffentlich (nicht Test) zur Athom-Zertifizierung eingereicht, `publish directly after approval` gesetzt — Freigabe steht zum Zeitpunkt dieses Log-Eintrags noch aus. |
| `0.4.6` | 2026-07-13 | `98c3a87` | Homey-Install | M5.5 | Stabile Geräte-Identität: `data.id` = Controller-Seriennummer (`HW_SERIAL_CARRIER`, herstellerbestätigt; ersetzt `crypto.randomUUID()`) → Homey blockt Doppel-Pairing desselben Reglers; fail-closed bei fehlender Serial (nur neue Pairings, Bestandsgeräte behalten ihre UUID). Alle nutzersichtbaren Fehlermeldungen lokalisiert (en/de, 9 Keys + Konsistenz-Test; 401/403 → eigene „Schreibzugriff verweigert"-Meldung). „encrypted store"-Doku-Korrektur (Homey garantiert keine Verschlüsselung). 5 SDD-Tasks + Whole-Branch-Review (fable): merge-ready. |
| `0.4.7` | 2026-07-14 | `bb5315d` | **Homey App Store — ÖFFENTLICH LIVE** (https://homey.app/de-de/app/de.neunbft.violet/Violet-Poolsteuerung/) | M5.5 | **0.4.5 von Athom abgelehnt** (Zertifizierungs-Feedback): Beschreibung zu lang/feature-listig, App-Bild war das wiederverwendete Treiber-Produktfoto; zusätzlich (unverbindlich) App-Name zu markenlastig, Icons mit gefüllten Flächen. Alle vier Punkte behoben: Beschreibung auf Einzeiler gekürzt („Protects your pool's water balance — before problems start." / DE-Pendant); App-Bild jetzt ein Komposit aus echtem Pool-Foto (nutzerbereitgestellt) + freigestelltem Regler-Foto (Schlagschatten, ca. 1/4 Bildbreite) statt Vektor-Illustration; App-Name gekürzt auf „Violet Pool Control"/„Violet Poolsteuerung" (BADU Blue bleibt im README); beide Icons als reine Strichgrafik ohne Füllflächen neu gebaut. Keine Funktionsänderung. `validate --level publish` PASS. **Zertifizierung von Athom bestätigt — App ist öffentlich live im Homey App Store.** |

| `0.5.0` | 2026-07-14 | `d532c3c` | Homey-Install | M5.7 | Config-basierte Autoconfig: `getConfig` als Zweitquelle der Feature-Erkennung — **gezielte Whitelist-Multi-Key-Queries statt `?ALL`** (live verifiziert: ohne Basic Auth lesbar, Secrets werden nie empfangen; Credentials nur als Restricted-Fallback). Per-Feature-Signalmatrix: Cover config-autoritativ in beide Richtungen (behebt den realen COVER_STATE-Geisterwert-False-Positive; Live-Smoke: Tile auf Referenzgerät entfernt), Solar/Heizung/Backwash/Refill nur positiv-additiv. NAMES_-Klartextlabels auf onewire-Sub-Sensoren („Schwimmbad" statt „Sensor 1"). CONFIGCHANGEMARKER-getriebener Re-Read + Re-Reconcile im selben Poll; Facts/Marker im Device-Store (Restart-sicher); empty-facts-Guard (SR-13), gedrosseltes Fehler-Logging (SR-16). Threat-Model-Delta SR-11…16. `npm test` 233 pass / fail 0; `validate --level publish` PASS; release-readiness PASS. |

| `0.5.1` | 2026-07-14 | `e94e5bb` | Homey-Install | M5.7 | Folgebuild: **Smart-Auto Wassertemperatur** — bei mehreren Temperaturfühlern wählt „Auto" jetzt den Kanal, dessen Violet-Name nach Pool aussieht (`schwimm/pool/becken/wasser`); manuelle Auswahl gewinnt, Ein-Kanal-Fallback unverändert. Behebt „Water temperature = -" bei 4 Fühlern (Live-Smoke: Kachel zeigt jetzt 29,8 °C = „Schwimmbad"/ow1). Dazu **Settings-Hints** (en+de) für die zuvor unerklärten Einstellungen (waterTempChannel, writeUsername, pumpWarmupSeconds, group_chlorine/group_eco, dosing-Schwelle, Pumpendrehzahl). `npm test` 239 pass/fail 0; `validate --level publish` PASS; release-readiness PASS. |

| `0.6.0` | 2026-07-15 | `f756d54` | Homey-Install | M5.8 | getReadings-Vervollständigung: **Messeingangs-Kacheln** `measure_adc.<n>`/`measure_impulse.<n>` config-gesteuert (nur `use=1`-Kanäle; Titel aus `NAMES_*`, Einheit aus `*_units`, Nachkommastellen aus `ANALOG_adcN_decimal` — auf Referenz-Hardware genau 2 neue Kacheln: Filterdruck [Bar], Anströmung [cm/s]). **Parser-Fixes:** `parseRangeToDays` versteht `h`-Suffix (33h→1.38 d, behebt die live stale CL-Reichweite-Kachel) + `>`-Präfix (>99d→99); gemeinsamer STATE-Parser für Array UND Pipe-String (`"0\|BLOCKED_BY_SENSOR_FAULT"`), nur `BLOCKED_BY_*` gilt als Block → `alarm_dosing_blocked`-False-Positive behoben (todo-Test entfroren). Diagnose-Annotation zeigt STATE-Blockiergründe an Pumpe/Heizung/Solar. Whitelist-Delta SR-11-konform (`NAMES_impulscount`, `ANALOG_adcN_decimal`). Keine Alarme (User-Entscheid, vertagt auf Trigger-Milestone). `npm test` 258 pass/fail 0/todo 0; typecheck clean; `validate --level publish` PASS; Whole-Branch-Review (fable) Ready-to-merge; /security-review keine Findings. |

| `0.6.1` | 2026-07-15 | `d1ca58d` | Homey-Install | M5.8 | Hotfix aus dem 0.6.0-Live-Smoke: **Upgrade-Pfad-Bug** — im Device-Store persistierte ConfigFacts der M5.7-Form (ohne Impuls-`name`/ADC-`decimals`) wurden ohne CONFIGCHANGEMARKER-Bewegung nie neu gelesen → Impuls-Kachel zeigte Fallback-Titel „Pulse input 1" statt „Anströmung". Fix: `FACTS_SCHEMA_VERSION`-Guard (Store-Facts älterer Schema-Version werden verworfen → einmaliger getConfig-Re-Read beim ersten Tick; getConfig-unreachable degradiert wie Fresh-Install). 2 neue Device-Tests (beide Richtungen). `npm test` 260 pass/fail 0/todo 0; typecheck clean; validate publish PASS; Reviewer-Verifikation „Ready to merge" bestätigt. |

| `0.6.2` | 2026-07-15 | `daeb5bb` | **Homey App Store (publiziert)** | M5.8 | Store-Publish der M5.8-Reihe, **code-identisch zu 0.6.1** (eigene Nummer wegen release-gate-Doppel-Release-Schutz — 0.6.1 war schon als Homey-Install geloggt). Kumulativer Store-Changelog en+de (User-approved), da Store-User von 0.4.7 direkt auf 0.6.2 springen: Config-Autoconfig + NAMES-Labels + Smart-Auto-Wassertemperatur (0.5.x) plus Messeingangs-Kacheln, Reichweiten-/STATE-Parser-Fixes und Facts-Schema-Guard (0.6.x). `npx homey app publish` vom User ausgeführt (interaktiv, „Update version?" → N); Kanal-/Zertifizierungsstatus über die Homey Developer Tools. |
| `0.7.0` | 2026-07-16 | `98c44b2` | Homey-Install (Branch, experimentell) | M6.1 | Inbound-NOTIFY-Alarme: eingebetteter HTTP-Listener (Port-Setting notifyPort, Default 22222) + Flow-Trigger „Ein Alarm wurde empfangen“ (Tokens errorcode/subject, optionaler Code-Filter). Installiert vom M6.1-Branch (Worktree, main unberuehrt); Live-Verifikation an der Violet (NOTIFY-Port-Umstellung 5080→22222) steht aus — danach Merge-Entscheidung per §9. Negativ-Check live OK (400 ohne ERRORCODE). |

**Zertifizierung von `0.4.7` läuft erneut bei Athom** (öffentlich, Freigabe ausstehend — 0.4.5 wurde abgelehnt, siehe Notiz oben). Nach Bestätigung: EN-Community-Thread editieren (Beta-Hinweis raus, finaler Store-Link), DE-Post auf poolsteuerung.de veröffentlichen, Cross-Links ergänzen, diese Zeile ggf. auf „live" aktualisieren. Write-Passwort-Rotation ist **erledigt** (2026-07-10).
