# M4.5 TypeScript-Typecheck (checkJs) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task (inline execution — die Tasks teilen Toolchain-Zustand und die tsc-Fehlerliste, Subagenten lohnen nicht). Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `tsc --noEmit` (checkJs, lenient) über den bestehenden JS-Code als drittes maschinelles Fehlersignal (neben `node --test` und `homey app validate`), verankert als Commit-Gate-Hook — ohne Build-Schritt, ohne Test-Latenz-Kosten, ohne Release.

**Architecture:** Drei Bausteine: (1) Checker-Toolchain (`tsconfig.checkjs.json` + devDeps + npm-Script `typecheck`), (2) die 16 bekannten lenient-Fehler fixen (`driver.js`-`onPair` zuerst; Runtime-Änderungen deployen erst mit M6), (3) PreToolUse-Commit-Gate-Hook `typecheck-gate.js` nach dem `check-version-sync.js`-Muster, per TDD. Kein `.ts`, kein `tsconfig.json` im Root, kein PostToolUse-Typecheck.

**Tech Stack:** TypeScript ^6 (nur Checker, kein Emit), `homey-apps-sdk-v3-types` via `@types/homey`-Alias, `@types/node@^18` (= Homey-Runtime-Major), `node:test`, Claude-Code-PreToolUse-Hooks.

**Spec:** `docs/superpowers/notes/2026-07-08-typescript-migration-evaluation.md` — verbindlich §4 (Pfad A), §5, §6.

## Global Constraints

- **KEIN `tsconfig.json` im Root** — die Checker-Config heißt exakt `tsconfig.checkjs.json` (tsconfig.json-Präsenz schaltet die Homey-CLI in den TS-Modus; `validate` bricht ohne build-Script, Spec §1).
- **`dependencies` in package.json bleibt exakt `{}`** — nur `devDependencies` kommen hinzu; `package-lock.json` wird committet (Spec §4A).
- **KEIN Version-Bump, kein Changelog-Eintrag, kein Release** — tooling-only (CLAUDE.md §8); Runtime-Fixes reisen mit M6 (Inbound-Alarme).
- **KEIN PostToolUse-Typecheck** (gemessen 6,6 s je Edit, auch inkrementell — Spec §3/§4A).
- **Abbruchkriterium (Spec §6):** Erfolg nur durch Aufweichen unterhalb `strict: false` erreichbar (checkJs partiell abschalten, `// @ts-nocheck`, Excludes) → Task stoppen, Befund im Entscheidungsdokument nachtragen. Fixes daher NUR als echte Code-/JSDoc-Korrekturen oder präzise `/** @type {…} */`-Casts in Tests.
- **Baseline am Umsetzungstag neu messen** (3 Läufe, Median): `node --test`-Wandzeit **B_test**, `validate --level publish`-Wandzeit **B_val** (Spec §6).
- **documenting-code-Skill** für jede angefasste Quelldatei; **TDD** für den Hook (Test in `test/hooks/`).
- **Git-Hygiene:** Feature-Branch von `main`; `git add` nur mit expliziten Pfaden — die fremden untracked Dateien (`LICENSE`, `README.md`, `docs/marketing/`, `docs/superpowers/specs/2026-07-07-community-launch-posts-design.md`) NICHT anfassen. Vor Push/Merge: `/code-review`, dann User fragen (CLAUDE.md §9).
- **Security by Design (CLAUDE.md §5): übersprungen mit Begründung** — kein neuer Angriffs-Surface: lokales Dev-Tooling, kein Netzwerk-Listener, kein Untrusted Input (Hook-stdin kommt vom Harness und ist fail-open behandelt), keine Credential-Pfade.
- **Friction sofort loggen:** `FRICTION:`-Log-Eintrag im M4.5-Dashboard-Eintrag, sobald sie auftritt (CLAUDE.md §7).
- CI-Verankerung des Typechecks ist **explizit M4.6** (GitHub-Actions), nicht Teil dieses Plans.

---

### Task 0: Branch + Plan committen

**Files:**
- Modify: `docs/dashboard/dashboard.html` (nur M4.5-Objekt: Step "Plan" abhaken, Log-Eintrag)
- Create: `docs/superpowers/plans/2026-07-08-m4.5-typescript-checkjs.md` (diese Datei)

- [ ] **Step 1: Feature-Branch anlegen**

```bash
git checkout -b m4.5-typecheck-checkjs
```

Expected: `Switched to a new branch 'm4.5-typecheck-checkjs'` (die uncommitteten Dashboard-Änderungen wandern mit).

- [ ] **Step 2: Dashboard — Step "Plan" auf done, Log-Eintrag "Plan geschrieben + committet", currentActivity aktualisieren**

- [ ] **Step 3: Commit (nur Plan + Dashboard)**

```bash
git add docs/superpowers/plans/2026-07-08-m4.5-typescript-checkjs.md docs/dashboard/dashboard.html
git commit -m "docs(m4.5): implementation plan + dashboard active"
```

---

### Task 1: Baseline messen (§6-Vorbedingung)

**Files:** keine (Messwerte landen im Dashboard-Log und in der §6-Ergebnistabelle von Task 7).

- [ ] **Step 1: B_test messen — 3 Läufe `node --test`, Median notieren**

```powershell
1..3 | ForEach-Object { (Measure-Command { node --test }).TotalSeconds }
```

Expected: 3 Werte in der Größenordnung 4–6 s (Spec §3: 4,6–4,7 s am 2026-07-08); alle Läufe `pass 91, fail 0`.

- [ ] **Step 2: B_val messen — 3 Läufe validate, Median notieren**

```powershell
1..3 | ForEach-Object { (Measure-Command { npx homey app validate --level publish }).TotalSeconds }
```

Expected: 3 Werte um ~8 s, exit 0 (Spec §3: 7,6 s).

- [ ] **Step 3: Referenzwert `npm test` einmal messen** (Kriterium 3 vergleicht `npm test` gegen 2×B_test)

```powershell
(Measure-Command { npm test }).TotalSeconds
```

- [ ] **Step 4: B_test/B_val als Log-Eintrag in den M4.5-Dashboard-Eintrag schreiben** (kein Commit nötig — wird mit Task 2 mitcommittet)

---

### Task 2: Checker-Toolchain (tsconfig + devDeps + npm-Script) — RED erwartet

> **Abweichung bei Ausführung (2026-07-08):** `npm i -D typescript` brach `validate` — homey-CLI 4.1.1 schaltet den TS-Modus über `devDependencies.typescript`, nicht über tsconfig-Präsenz (systematic-debugging, CLI-Quelle gelesen). Umgesetzt als Alias **`typescript-checkjs@npm:typescript@^6`** + Invarianten-Test `test/toolchain.test.js`; Details im Eval-Doc §1 Nachtrag. Außerdem `moduleResolution: "node"` → `module: "node18"` (TS-6-Deprecation TS5107). Der Hook in Task 6 muss `typescript-checkjs/lib/tsc.js` (mit Fallback `typescript/lib/tsc.js`) auflösen.

**Files:**
- Create: `tsconfig.checkjs.json`
- Modify: `package.json` (devDependencies via npm, Script `typecheck`)
- Modify/Create: `package-lock.json` (via npm; existiert bereits untracked)

**Interfaces:**
- Produces: `npm run typecheck` ≙ `tsc -p tsconfig.checkjs.json` — von Task 3–7 und vom Hook (Task 6) benutzt.

- [ ] **Step 1: devDependencies installieren**

```bash
npm install --save-dev typescript "@types/homey@npm:homey-apps-sdk-v3-types" "@types/node@^18"
```

Expected: exit 0, `package.json` erhält die 3 devDependencies, `dependencies` bleibt `{}`.

- [ ] **Step 2: `tsconfig.checkjs.json` anlegen** (JSONC-Kommentare sind ok: tsc parst sie, json-guard guarded diese Datei nicht)

```jsonc
// Checker-only config for `npm run typecheck` (M4.5, Pfad A — evaluation doc §4A/§6).
// Deliberately NOT named tsconfig.json: its presence flips the Homey CLI into TS mode
// and `validate` then fails without a build script (doc §1, spike-verified).
// `types` is explicit because TS 6 no longer auto-includes installed @types (doc §1 gotcha).
{
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "module": "commonjs",
    "moduleResolution": "node",
    "target": "es2022",
    "lib": ["es2022"],
    "types": ["node", "homey"],
    "resolveJsonModule": true,
    "skipLibCheck": true
  },
  "include": ["app.js", "lib/**/*.js", "drivers/**/*.js", "test/**/*.js"]
}
```

- [ ] **Step 3: npm-Script ergänzen** — in `package.json` `"scripts"`:

```json
"typecheck": "tsc -p tsconfig.checkjs.json"
```

(Edit von package.json triggert json-guard — Datei muss striktes JSON bleiben; Script-Eintrag programmatisch oder sehr sorgfältig editieren.)

- [ ] **Step 4: RED verifizieren — Typecheck läuft und findet die bekannten Fehler**

```bash
npx tsc -p tsconfig.checkjs.json > "$SCRATCHPAD/tsc-lenient-errors.txt"; echo "exit=$?"
```

Expected: exit ≠ 0, **~16 Fehler** (Spec §3: driver.js 1×TS2416, lib 2, test 13; device.js/app.js 0). Exakte Liste im Scratchpad sichern — sie steuert Task 3–5. Weicht die Zahl ab (TS-Minor-Drift): Liste zählt, Abweichung im Dashboard-Log notieren.

- [ ] **Step 5: Umgebung unverändert verifizieren**

```bash
ls tsconfig.json 2>/dev/null; npm test; npx homey app validate --level publish
```

Expected: kein `tsconfig.json` im Root; Tests `pass ≥ 91, fail 0`; validate exit 0 in ~B_val (kein TS-Schritt).

- [ ] **Step 6: Commit**

```bash
git add tsconfig.checkjs.json package.json package-lock.json
git commit -m "chore(typecheck): checkJs toolchain — tsconfig.checkjs.json + typescript devDeps (M4.5 Pfad A)"
```

---

### Task 3: driver.js `onPair`-Befund klären + fixen (ZUERST, per Spec)

**Files:**
- Modify: `drivers/pool/driver.js:59` (`onPair(session)`)
- Read: `node_modules/@types/homey/lib/Driver.d.ts` (SDK-Signatur)

**Interfaces:**
- Produces: unveränderte Pairing-Semantik; `onPair` wird `async` (erwartet). Kein Aufrufer im Repo ruft `onPair` direkt auf (nur Homey-Runtime).

- [ ] **Step 1: SDK-Signatur nachschlagen**

```bash
grep -n "onPair" node_modules/@types/homey/lib/Driver.d.ts
```

Expected: `onPair(session: PairSession): Promise<void>;` (async deklariert).

- [ ] **Step 2: Befund entscheiden — Bug vs. Typen-Strenge**

Bewertungsmaßstab: Ein echter Bug läge vor, wenn die Sync-Variante zur Laufzeit anderes Verhalten erzeugt (z. B. Homey ein Promise-Interface auf dem Rückgabewert aufruft). Erwartung: **Typen-Strenge** — die Handler-Registrierung via `session.setHandler(...)` ist in beiden Varianten synchron abgeschlossen, `await undefined` ist ein No-op; `async` ändern das Verhalten nicht. **Falls doch Belege für einen Runtime-Bug auftauchen: STOPP → superpowers:systematic-debugging, Befund im Entscheidungsdokument + Dashboard-Log dokumentieren.**

- [ ] **Step 3: Fix — Methode async machen, Decision-Kommentar (documenting-code)**

```js
  // async to match the SDK's declared onPair signature (checkJs TS2416, M4.5 eval doc
  // §3) — typing strictness, not a runtime bug: handler registration stays synchronous
  // and Homey awaits the returned promise either way.
  async onPair(session) {
```

- [ ] **Step 4: GREEN verifizieren**

```bash
npx tsc -p tsconfig.checkjs.json 2>&1 | grep -c "error TS"; npm test
```

Expected: Fehlerzahl um 1 gesunken (TS2416 weg), Tests `fail 0`.

- [ ] **Step 5: Commit**

```bash
git add drivers/pool/driver.js
git commit -m "fix(driver): async onPair to match SDK signature (checkJs TS2416, no runtime change)"
```

---

### Task 4: lib-Fixes — JSDoc-Contract vs. Code-Default (2 Befunde)

**Files:**
- Modify: `lib/FeatureGroups.js:142-145` (JSDoc von `buildM2Updates`)
- Modify: `lib/WriteClient.js:66` (`buildWriteUrl`-Default)

**Interfaces:**
- Produces: `buildM2Updates(raw, opts?)` — opts jetzt auch laut JSDoc optional (Runtime unverändert). `buildWriteUrl(host, cmd)` — cmd jetzt auch laut Signatur Pflicht (JSDoc war schon so); alle Aufrufer (device.js `_control`→`sendWrite`, Tests) übergeben cmd immer — verifiziert.

- [ ] **Step 1: `buildM2Updates` — JSDoc an die realen Defaults angleichen** (beide Props haben Destructuring-Defaults, `opts` selbst hat `= {}` → Props optional markieren)

```js
 * @param {object} raw Parsed getReadings payload.
 * @param {{dosingChannels?: string[], dosingLowThresholdDays?: number}} [opts]
 * @returns {Object<string, *>} capId (incl. `<base>.<ch>` sub-ids) → value.
```

(PostToolUse-Hook `run-matching-test` läuft dabei automatisch `test/FeatureGroups.test.js` — muss grün bleiben; das ist zugleich Vorab-Evidenz für §6-Kriterium 7.)

- [ ] **Step 2: `buildWriteUrl` — vakantes `= {}`-Default entfernen** (der JSDoc-Contract „target/state Pflicht" ist korrekt; das Default `{}` widerspricht ihm und existierte nur defensiv. Runtime-Differenz nur beim contract-widrigen Aufruf ohne cmd: TypeError statt RangeError — kein Repo-Aufrufer und kein Test tut das; Fix reist mit M6.)

```js
function buildWriteUrl(host, { target, scene, state, args = {} }) {
```

Dazu Decision-Kommentar an der Funktion ergänzen (documenting-code):

```js
 * @throws {RangeError} on unknown target, invalid scene/state, or out-of-range/non-finite arg.
 */
// cmd has no `= {}` default on purpose: the JSDoc contract requires target/state, and a
// defensive empty default contradicted it (checkJs finding, M4.5 eval doc §3).
function buildWriteUrl(host, { target, scene, state, args = {} }) {
```

- [ ] **Step 3: GREEN verifizieren — lib fehlerfrei, Tests grün**

```bash
npx tsc -p tsconfig.checkjs.json 2>&1 | grep "lib/"; npm test
```

Expected: keine lib/-Fehler mehr (Gesamtliste jetzt nur noch test/-Fehler); `fail 0`.

- [ ] **Step 4: Commit**

```bash
git add lib/FeatureGroups.js lib/WriteClient.js
git commit -m "fix(lib): align JSDoc contracts with runtime defaults (checkJs findings)"
```

---

### Task 5: test/-Fixes (~13 Befunde) — Stub-Casts, kein Aufweichen

**Files:**
- Modify: `test/WriteClient.test.js` (fetch-Stubs Zeilen 75/90/107, err-Prädikat Zeilen 94–98; weitere laut Task-2-Fehlerliste)
- Modify: ggf. weitere test/*.js laut Fehlerliste aus Task 2 — mit denselben Mustern.

**Interfaces:** keine (nur Tests; Runtime unberührt).

- [ ] **Step 1: fetch-Stubs typisieren — Muster (3 Stellen in WriteClient.test.js):**

```js
global.fetch = /** @type {any} */ (async (url, opts) => { calls.push({ url, opts }); return { ok: true, status: 200, text: async () => 'OK\nPUMP\non' }; });
```

(Cast auf `any` ist hier ehrlich: der Stub ist bewusst kein vollständiges `Response`; das Abbruchkriterium verbietet Config-Aufweichen, nicht präzise Einzel-Casts in Tests.)

- [ ] **Step 2: `err.message` auf `unknown` — Prädikat-Muster:**

```js
      (err) => {
        const msg = /** @type {Error} */ (err).message;
        assert.ok(/HTTP 401/.test(msg));
        assert.ok(!/sekret/.test(msg) && !/Basic /.test(msg));
        return true;
      },
```

- [ ] **Step 3: Restliche Fehler aus der Task-2-Liste mit denselben zwei Mustern fixen** (minimaler `/** @type {…} */`-Cast am Befundort; NIE `@ts-nocheck`/`@ts-ignore`, NIE tsconfig anfassen — Abbruchkriterium).

- [ ] **Step 4: GREEN verifizieren — §6-Kriterium 1 erreicht**

```bash
npx tsc -p tsconfig.checkjs.json; echo "exit=$?"; npm test
```

Expected: `exit=0`, Tests `pass ≥ 91, fail 0`.

- [ ] **Step 5: Commit**

```bash
git add test/
git commit -m "test: type-safe fetch stubs + error casts for checkJs (no config weakening)"
```

---

### Task 6: Commit-Gate-Hook `typecheck-gate.js` (TDD)

**Files:**
- Test: `test/hooks/typecheck-gate.test.js` (neu)
- Create: `.claude/hooks/typecheck-gate.js`
- Modify: `.claude/settings.json` (Hook registrieren, PreToolUse/Bash neben check-version-sync)

**Interfaces:**
- Consumes: Hook-stdin-JSON `{ tool_input: { command }, cwd }` (Harness-Payload, wie check-version-sync.js).
- Produces: exit 0 (pass/fail-open) oder exit 2 + stderr (Block mit tsc-Output). Design: fail-open ohne `tsconfig.checkjs.json` oder ohne auflösbares `typescript` (portabel für fremde Repos); tsc wird via `require.resolve('typescript/lib/tsc.js', { paths: [cwd] })` + `process.execPath` gestartet (kein npx-Overhead, Windows-sicher ohne shell). M4.6 kombiniert dieses Gate später mit dem Test-Gate.

- [ ] **Step 1: Failing Test schreiben — `test/hooks/typecheck-gate.test.js`**

```js
'use strict';

// Smoke test for .claude/hooks/typecheck-gate.js (PreToolUse Bash) — the gate blocks
// `git commit` (exit 2) while `tsc -p tsconfig.checkjs.json` is red in the guarded
// project, passes green projects and non-commit commands, and fails open (exit 0) on
// its own errors (malformed stdin, no checker config, no typescript install) — M4.5
// eval doc §4A "Commit-Gate". Mirrors the compose-guard/json-guard hook tests.
// Assumes devDependencies are installed (node_modules/typescript present).

const { test } = require('node:test');
const assert = require('node:assert');
const { spawnSync } = require('node:child_process');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');

const HOOK = path.join(__dirname, '..', '..', '.claude', 'hooks', 'typecheck-gate.js');
const REPO_NODE_MODULES = path.join(__dirname, '..', '..', 'node_modules');

// Throwaway project with a one-file checker config. `srcText` decides red vs green.
// node_modules is junction-linked to this repo's so `typescript` resolves without a
// per-test npm install (junction: works without admin rights on Windows).
function makeProject(srcText) {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'typecheck-gate-'));
  fs.writeFileSync(path.join(dir, 'tsconfig.checkjs.json'), JSON.stringify({
    compilerOptions: { allowJs: true, checkJs: true, noEmit: true, strict: false, types: [] },
    include: ['src.js'],
  }));
  fs.writeFileSync(path.join(dir, 'src.js'), srcText);
  fs.symlinkSync(REPO_NODE_MODULES, path.join(dir, 'node_modules'), 'junction');
  return dir;
}

function runHook(command, cwd, raw) {
  const payload = raw !== undefined
    ? raw
    : JSON.stringify({ tool_name: 'Bash', cwd, tool_input: { command } });
  const r = spawnSync(process.execPath, [HOOK], { input: payload, encoding: 'utf8' });
  return { code: r.status, err: (r.stderr || '').trim() };
}

const RED_SRC = "'use strict';\n/** @type {number} */\nconst n = 'not a number';\nmodule.exports = n;\n";
const GREEN_SRC = "'use strict';\n/** @type {number} */\nconst n = 1;\nmodule.exports = n;\n";

test('typecheck-gate: git commit with red typecheck → BLOCK with tsc output', () => {
  const dir = makeProject(RED_SRC);
  const { code, err } = runHook('git commit -m "x"', dir);
  assert.strictEqual(code, 2, err);
  assert.match(err, /src\.js/); // the tsc finding must reach the model as fix guidance
});

test('typecheck-gate: git commit with green typecheck → PASS', () => {
  const dir = makeProject(GREEN_SRC);
  const { code, err } = runHook('git commit -m "x"', dir);
  assert.strictEqual(code, 0, err);
});

test('typecheck-gate: non-commit command → PASS without running tsc', () => {
  const dir = makeProject(RED_SRC); // red on purpose: must not even be looked at
  const { code } = runHook('git status', dir);
  assert.strictEqual(code, 0);
});

test('typecheck-gate: repo without tsconfig.checkjs.json → PASS (not ours to gate)', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'typecheck-gate-'));
  const { code } = runHook('git commit -m "x"', dir);
  assert.strictEqual(code, 0);
});

test('typecheck-gate: typescript not resolvable → PASS (fail-open)', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'typecheck-gate-'));
  fs.writeFileSync(path.join(dir, 'tsconfig.checkjs.json'), '{}');
  const { code } = runHook('git commit -m "x"', dir);
  assert.strictEqual(code, 0);
});

test('typecheck-gate: malformed stdin → PASS (fail-open)', () => {
  const { code } = runHook(null, undefined, 'not json{');
  assert.strictEqual(code, 0);
});
```

- [ ] **Step 2: RED verifizieren**

```bash
node --test test/hooks/typecheck-gate.test.js
```

Expected: FAIL — Hook-Datei existiert nicht (`spawnSync` liefert exit ≠ 0/2-Mismatch bzw. MODULE_NOT_FOUND auf stderr).

- [ ] **Step 3: Hook implementieren — `.claude/hooks/typecheck-gate.js`**

```js
'use strict';

// PreToolUse hook (matcher: Bash) — blocks `git commit` while `tsc -p
// tsconfig.checkjs.json` is red, so type errors can't land in history (M4.5 —
// eval doc §4A "Commit-Gate": ~+7 s per commit, deliberately NOT a PostToolUse
// per-edit check, measured 6.6 s each). Pattern mirrors check-version-sync.js:
// fail open on our own errors, exit 2 only on a real finding. tsc is resolved
// from the guarded repo's own node_modules (no npx, shell-free for Windows);
// repos without the checker config or without typescript are never blocked.

const fs = require('fs');
const path = require('path');
const { spawnSync } = require('child_process');

let payload = '';
process.stdin.on('data', (chunk) => { payload += chunk; });
process.stdin.on('end', () => {
  let input;
  try {
    input = JSON.parse(payload);
  } catch {
    process.exit(0); // can't parse -> fail open, don't block on our own error
  }

  const command = (input.tool_input && input.tool_input.command) || '';
  if (!/\bgit\s+commit\b/.test(command)) {
    process.exit(0); // only care about commits
  }

  const cwd = input.cwd || process.cwd();
  if (!fs.existsSync(path.join(cwd, 'tsconfig.checkjs.json'))) {
    process.exit(0); // no checker config -> not ours to gate
  }

  let tscPath;
  try {
    tscPath = require.resolve('typescript/lib/tsc.js', { paths: [cwd] });
  } catch {
    process.exit(0); // typescript not installed -> fail open
  }

  const r = spawnSync(process.execPath, [tscPath, '-p', 'tsconfig.checkjs.json'], { cwd, encoding: 'utf8' });
  if (r.status === 0 || r.status === null) {
    process.exit(0); // green, or tsc itself failed to spawn -> fail open
  }

  console.error(
    'typecheck-gate: tsc -p tsconfig.checkjs.json failed — fix the type errors below '
    + '(or run "npm run typecheck") before committing.\n'
    + [r.stdout, r.stderr].filter(Boolean).join('\n').trim()
  );
  process.exit(2); // block the commit
});
```

- [ ] **Step 4: GREEN verifizieren**

```bash
node --test test/hooks/typecheck-gate.test.js
```

Expected: `pass 6, fail 0`.

- [ ] **Step 5: Hook registrieren — `.claude/settings.json`, PreToolUse/Bash-Block:**

```json
{
  "matcher": "Bash",
  "hooks": [
    { "type": "command", "command": "node .claude/hooks/check-version-sync.js" },
    { "type": "command", "command": "node .claude/hooks/typecheck-gate.js" }
  ]
}
```

(Registrierung greift erst beim nächsten Session-Start — in dieser Session zählt die Direktaufruf-Verifikation aus den Tests; zusätzlich einmal real gegen dieses Repo:)

```bash
echo '{"tool_name":"Bash","cwd":"'"$(pwd -W 2>/dev/null || pwd)"'","tool_input":{"command":"git commit -m test"}}' | node .claude/hooks/typecheck-gate.js; echo "exit=$?"
```

Expected: `exit=0` (Repo ist zu diesem Zeitpunkt typecheck-grün), Dauer ~7 s.

- [ ] **Step 6: Voller Testlauf + Typecheck (der neue Test ist selbst im checkJs-Include)**

```bash
npm test; npx tsc -p tsconfig.checkjs.json; echo "exit=$?"
```

Expected: `pass ≥ 97, fail 0`; typecheck exit 0.

- [ ] **Step 7: Commit**

```bash
git add test/hooks/typecheck-gate.test.js .claude/hooks/typecheck-gate.js .claude/settings.json
git commit -m "feat(hooks): typecheck-gate blocks git commit on red tsc (TDD, M4.5)"
```

---

### Task 7: §6-Kriterien-Sweep, Dashboard-Abschluss, /code-review, §9-Frage

**Files:**
- Modify: `docs/dashboard/dashboard.html` (M4.5-Objekt: Steps, Log inkl. §6-Ergebnistabelle-Kurzform, status done, commit-SHA, currentActivity null; updatedAt)

- [ ] **Step 1: Alle 8 §6-Kriterien maschinell durchprüfen** (Kriterium 5 kanonisch mit `-p tsconfig.checkjs.json` — ein argumentloses `npx tsc` ist ohne Root-tsconfig gar nicht lauffähig):

```bash
# K1: npx tsc -p tsconfig.checkjs.json                     → exit 0
# K2: node --test                                          → fail 0, pass ≥ 91
# K3: Measure-Command { npm test }                         → ≤ 2 × B_test
# K4: Measure-Command { npx homey app validate --level publish } → exit 0, ≤ 2 × B_val
# K5: npx tsc -p tsconfig.checkjs.json && npx homey app build
#     git status --porcelain app.json .homeycompose/app.json → leer
# K6: kein tsconfig.json im Root; build-Output ohne TS-Schritt, exit 0
# K7: Kommentar-Touch-Edit an lib/Lsi.js → PostToolUse-Hook läuft test/Lsi.test.js; Edit revertieren
# K8: package.json enthält weiterhin "dependencies": {}
```

Expected: alle 8 PASS. Bei FAIL: prüfen ob Abbruchkriterium (nur K1-via-Aufweichen, K3, K4) → STOPP, Befund in `docs/superpowers/notes/2026-07-08-typescript-migration-evaluation.md` §6 nachtragen, Dashboard-Log, Status quo behalten (Branch nicht mergen).

- [ ] **Step 2: Ergebnistabelle (K1–K8 mit Messwerten) als Dashboard-Log-Eintrag; Steps "Implementierung (TDD)" + "Validate + Push" abhaken; status "done", finishedAt, commit (Short-SHA des letzten Commits), currentActivity null, prompt null, updatedAt bumpen** — Hinweis im Log: Push/Merge pending User-Entscheid (§9).

- [ ] **Step 3: Dashboard-Commit**

```bash
git add docs/dashboard/dashboard.html
git commit -m "docs(dashboard): M4.5 done — checkJs green, §6 criteria PASS"
```

- [ ] **Step 4: /code-review über den Branch-Diff gegen main starten** (CLAUDE.md §9)

- [ ] **Step 5: User fragen (§9):** kein Critical Issue → direkt auf origin/main pushen (FF) vs. Branch pushen + PR. **Auf explizites Ja warten — nicht selbst pushen/mergen.**

---

## Self-Review (durchgeführt beim Schreiben)

- **Spec-Abdeckung:** §4A Config ✓(T2), devDeps+Lockfile ✓(T2), Script ✓(T2), 16 Fixes mit onPair zuerst ✓(T3–T5), Commit-Gate ✓(T6), CI → explizit M4.6 (Constraint), §6-Kriterien + Abbruch ✓(T1/T7), kein Bump ✓(Constraints), Dashboard-Protokoll ✓(T0/T7), §9 ✓(T7).
- **Platzhalter:** keine — alle Code-Steps tragen den echten Code; Task 5 Schritt 3 arbeitet bewusst gegen die in Task 2 maschinell erzeugte Fehlerliste mit zwei konkret definierten Fix-Mustern und harter Abbruch-Leitplanke.
- **Typ-/Namens-Konsistenz:** `tsconfig.checkjs.json`, `npm run typecheck`, `typecheck-gate.js`, `test/hooks/typecheck-gate.test.js` durchgängig; Hook-stdin-Shape identisch zu check-version-sync.js.
