# M4.9 Framework-Extraktion (agentic-loop-framework) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Portabler Skill `agentic-loop-framework` in eigenem privaten Repo (User-Entscheid a) + Spiegelregel/Drift-Check im VioletApp-Repo + Prompt-Konsistenz-Sweep — Spec: `docs/superpowers/specs/2026-07-09-m4.9-framework-extraction.md`.

**Architecture:** Plugin-Layout nach Muster des Settings-Repos (marketplace.json → plugin/ → skills/agentic-loop-framework/). `templates/` ist ein 1:1 kopierfähiger Projektbaum (Trockentest = kopieren + parsen + Smoke-Test laufen lassen). Generische Templates werden aus den EXAKTEN VioletApp-Quelldateien abgeleitet (präzise Transformationsregeln statt Neuschreiben — DRY); Homey-Bausteine getrennt unter `homey/`.

**Tech Stack:** gh CLI (Repo-Anlage), Node (`node:test` für den Trockentest), Markdown/JSON-Templates.

## Global Constraints

- Tooling-only: KEIN Version-Bump, kein Release in VioletApp (§8).
- Jede Vorlage frei von VioletApp-/Violet-/Homey-Bezügen (templates/) bzw. VioletApp-Bezügen (homey/) — Grep-Check vor Commit: `violet|Violet|de\.neunbft` (Treffer nur in CHANGELOG-Quellenangaben erlaubt).
- Transformationsregeln je Template EXAKT befolgen; Struktur/Verhalten der Hooks NICHT ändern (nur Namen/Wortlaut generalisieren) — die Telemetrie-/spawn-env-Zeilen bleiben.
- VioletApp-Edits chirurgisch (Sweep: NUR `prompt`-Felder + Checkpoint-`steps[]`); nach jedem dashboard-Edit node-Eval-Parse.
- Git-Aktionen in ALLEN drei Repos (VioletApp, Settings, Framework) erst nach /code-review + expliziter Freigabe (§9); Ausnahme: Commits IM neuen Framework-Repo während des Aufbaus (lokal, unge-pusht) sind Arbeitsstände — der PUSH ist die §9-Aktion.
- documenting-code: Hook-Vorlagen behalten ihre Header (Spec-Refs werden auf die Framework-CHANGELOG-Quelle umgestellt: `(extrahiert aus VioletApp M4.x — siehe CHANGELOG)`).

---

### Task 1: Framework-Repo anlegen (Plugin-Struktur + SKILL.md + CHANGELOG)

**Files (neues Repo `C:/Users/TorstenSturm/source/repos/skill-agentic-loop-framework`):**
- Create: `.claude-plugin/marketplace.json`, `plugin/.claude-plugin/plugin.json`, `README.md`, `plugin/skills/agentic-loop-framework/SKILL.md`, `plugin/skills/agentic-loop-framework/CHANGELOG.md`

- [ ] **Step 1.1:** Settings-Repo-Schema lesen (`skill-ClaudeCode-general-settings/.claude-plugin/marketplace.json` + `plugin/.claude-plugin/plugin.json`) — die neuen JSONs übernehmen exakt dieses Schema (Felder/Struktur), nur Werte ersetzt: Marketplace-Name `agentic-loop-framework`, Plugin-Name `agentic-loop-framework`, Beschreibung „Loop-getriebenes agentisches Entwicklungs-Framework — Bootstrap-Skill + Templates (Karpathy-Kern, Dashboard-Protokoll, Gate-Hooks, Checkpoint-Skill) + Homey-Modul".
- [ ] **Step 1.2:** Repo anlegen:

```powershell
gh repo create tnsturm/skill-agentic-loop-framework --private --clone
# klont nach ./skill-agentic-loop-framework im aktuellen Verzeichnis — vorher nach C:/Users/TorstenSturm/source/repos wechseln
```

- [ ] **Step 1.3:** JSONs programmatisch schreiben (node + JSON.stringify, json-guard-Regel), README.md (Kurzbeschreibung + Install-Zeile `claude plugin marketplace add tnsturm/skill-agentic-loop-framework` + Verweis auf SKILL.md/templates/homey).
- [ ] **Step 1.4:** `SKILL.md` schreiben — Frontmatter:

```yaml
---
name: agentic-loop-framework
description: Bootstrap eines loop-getriebenen agentischen Entwicklungs-Frameworks in einem beliebigen Repo — verifizierte, selbstkorrigierende Loops (Gates, Dashboard, Checkpoints, Telemetrie) statt Prompt-für-Prompt-Arbeit. Trigger: "agentic loop aufsetzen", "Framework-Bootstrap", "verifizierte Loops einrichten", neues/bestehendes Projekt auf Milestone-Loops umstellen.
---
```

Inhalt = Prompt A aus `skill-ClaudeCode-general-settings/prompts/agentic-loop-bootstrap.de.md`, umgebaut von Copy-Paste-Prompt zu Skill-Anleitung mit diesen EXAKTEN Transformationen: (T1) „## Kontext (von mir ausgefüllt)"-Block → Abschnitt „## Vorab klären (falls nicht im Auftrag genannt)" mit denselben 4 Punkten als Fragen; (T2) jede Phase, deren Artefakt als Template vorliegt, verweist statt Neuerzeugung auf den Baum: Phase 1 → „kopiere `templates/` (CLAUDE.md, docs/dashboard/, .claude/settings.json, .claude/hooks/lib/) und passe Platzhalter an", Phase 3 → „`templates/.claude/hooks/test-gate.js` + `templates/test/hooks/test-gate.test.js` übernehmen; bei Nicht-npm-Stack die im Hook-Kopf dokumentierte Stelle anpassen", Phase 5 → „`templates/.claude/skills/milestone-checkpoint/SKILL.md` übernehmen und den Skill-Quellen-Platzhalter füllen", Phase 6 → „dieses Repo IST der Skill — nur die Spiegelregel im Projekt-CLAUDE.md verankern (Vorlage in templates/CLAUDE.md §Source-of-Truth)"; (T3) Plattform-Hinweis: „Für Homey-Apps: `homey/README.md` = Zusatzblock (Prompt B), Bausteine in `homey/`"; (T4) Dauerregeln unverändert übernehmen; (T5) Hinweis auf die historischen Bootstrap-Prompts (de/en) im Settings-Repo als Seed-Referenz.

- [ ] **Step 1.5:** `CHANGELOG.md`:

```markdown
# Changelog — agentic-loop-framework

## 0.1.0 (2026-07-09)

Initiale Extraktion aus dem VioletApp-Projekt (M0–M4.8), M4.9 der Loop-Hardening-Reihe.
Quellen je Artefakt:
- SKILL.md ← prompts/agentic-loop-bootstrap.de.md (Settings-Repo, Stand 2026-07-07), generalisiert.
- templates/CLAUDE.md ← VioletApp CLAUDE.md (§0–§11, Karpathy-Kern via multica-ai/andrej-karpathy-skills).
- templates/docs/dashboard/dashboard.html ← VioletApp-Renderer (Datenblock geleert).
- templates/.claude/hooks/{test-gate.js,lib/log.js,lib/spawn-env.js} + test/hooks/test-gate.test.js ← VioletApp M4.6/M4.8.
- templates/.claude/skills/milestone-checkpoint/SKILL.md ← VioletApp M4.8-Stand + M4.9-Drift-Check.
- homey/ ← VioletApp HOMEY.md, 4 Guard-Hooks (M3–M4.5), release-readiness-Subagent, Homey-Allowlist.
```

- [ ] **Step 1.6:** Commit im neuen Repo: `git add -A && git commit -m "feat: Skill-Geruest — Plugin-Struktur, SKILL.md (Phasen 0-7 + Dauerregeln), CHANGELOG 0.1.0"`

---

### Task 2: templates/-Baum (generisch)

**Files (unter `plugin/skills/agentic-loop-framework/templates/`):**
- Create: `CLAUDE.md`, `docs/dashboard/dashboard.html`, `docs/dashboard/versions.md`, `docs/dashboard/triage-inbox.md`, `.claude/settings.json`, `.claude/hooks/test-gate.js`, `.claude/hooks/lib/log.js`, `.claude/hooks/lib/spawn-env.js`, `.claude/skills/milestone-checkpoint/SKILL.md`, `test/hooks/test-gate.test.js`, `package.json`

**Transformationsregeln (Quelle → Template):**

- [ ] **Step 2.1:** `CLAUDE.md` ← VioletApp `CLAUDE.md`, Änderungen: „## Project Extensions"-Block (Homey-Verweis) ersetzen durch Platzhalter-Zeile `<!-- Projekt-Extensions: Plattform-Datei per §6 referenzieren, z. B. @<PLATFORM>.md -->`; den Abschnitt „Claude-Code-Settings: Skill = Source of Truth" generalisieren (Repo-Namen → `<euer-Settings-Skill-Repo>` / `skill-agentic-loop-framework`, Homey-Beispiele → `<plattformspezifische Hooks/Allowlist>`); triage-inbox- und todo-Test-Regeln BEHALTEN (generisch); sonst 1:1.
- [ ] **Step 2.2:** `docs/dashboard/dashboard.html` ← VioletApp-Datei, Datenblock ersetzt durch:

```js
window.DASHBOARD_STATUS = {
  project: "<Projektname>",
  updatedAt: "<YYYY-MM-DD>",
  milestones: []
};
```

Renderer darunter byte-identisch übernehmen (nie editieren). Titel-/Header-Strings im HTML-Kopf: Projektname → `<Projektname>`-Platzhalter.
- [ ] **Step 2.3:** `docs/dashboard/versions.md` — Kopfsatz (eine Zeile Zweck) + Tabellen-Skelett `| Version | Datum | Commit | Ziel | Milestone | Notiz |` + Trennzeile, keine Datenzeilen. `docs/dashboard/triage-inbox.md` ← VioletApp-Datei: Kopf behalten (Routinen-Name → `<projekt>-nightly-triage`), Abschnitte `## Offen` / `## Neu (<Datum>)` / `## Erledigt (<Datum>)` leer.
- [ ] **Step 2.4:** `.claude/settings.json` (programmatisch bauen):

```json
{
  "hooks": {
    "PreToolUse": [
      { "matcher": "Bash|PowerShell", "hooks": [ { "type": "command", "command": "node .claude/hooks/test-gate.js" } ] },
      { "matcher": "Edit|Write", "hooks": [] }
    ],
    "PostToolUse": [ { "matcher": "Edit|Write", "hooks": [] } ],
    "Stop": [ { "hooks": [] } ]
  },
  "permissions": {
    "allow": [
      "Bash(git fetch *)",
      "Bash(git merge-base *)"
    ]
  }
}
```

- [ ] **Step 2.5:** Hooks 1:1 kopieren aus VioletApp: `.claude/hooks/test-gate.js`, `.claude/hooks/lib/log.js`, `.claude/hooks/lib/spawn-env.js`, `test/hooks/test-gate.test.js` — einzige Änderungen: (a) im Datei-Header die VioletApp-Spec-Pfade ersetzen durch `(extrahiert aus VioletApp M4.6/M4.8 — siehe CHANGELOG des agentic-loop-framework)`; (b) im test-gate.js-Kopf einen Absatz ergänzen: „Nicht-npm-Stacks: `scripts.test`-Auflösung unten durch das projektübliche SCHNELLE Testkommando ersetzen (Laufzeitbudget ≤30 s)."; (c) KEINE Verhaltensänderung (Telemetrie-Zeilen bleiben — lib/log.js ist Teil des Baums).
- [ ] **Step 2.6:** `package.json` (macht den Baum test-lauffähig):

```json
{
  "name": "agentic-loop-template",
  "version": "0.0.0",
  "private": true,
  "scripts": { "test": "node --test" }
}
```

- [ ] **Step 2.7:** `.claude/skills/milestone-checkpoint/SKILL.md` ← VioletApp-Datei NACH Task 4 des VioletApp-Teils (enthält dann schon den Drift-Check-Schritt) — Transformation: Abschnitt „Schritt 3: Skill-Quellen prüfen" ersetzen durch Platzhalter-Anleitung („projektabhängig: externe Skill-/Referenz-Quellen listen; Muster: git fetch + log HEAD..origin, bei Update pull + Kopie; nicht automatisierbare Quellen nur melden"); Homey-/Violet-Namen raus. HINWEIS Reihenfolge: dieser Step läuft NACH Task 4 (VioletApp-Checkpoint-Skill zuerst erweitern, dann extrahieren).
- [ ] **Step 2.8:** Grep-Check `violet|Violet|de\.neunbft|Homey|homey` über `templates/` → 0 Treffer (Homey-Wörter gehören nur in `homey/`). Commit: `git add templates && git commit -m "feat(templates): generischer Projektbaum (CLAUDE.md, Dashboard, settings, test-gate+lib, checkpoint-Skill)"`

---

### Task 3: homey/-Modul

**Files (unter `plugin/skills/agentic-loop-framework/homey/`):**
- Create: `README.md`, `HOMEY.md`, `hooks/compose-guard.js`, `hooks/json-guard.js`, `hooks/check-version-sync.js`, `hooks/secrets-guard.js`, `allowlist.json`, `agents/release-readiness.md`

- [ ] **Step 3.1:** `README.md` = Prompt-B-Inhalt (`agentic-loop-bootstrap.de.md`, Abschnitt PROMPT B) als Einbau-Anleitung umformuliert (welche Datei wohin: HOMEY.md → Repo-Root; hooks/* → .claude/hooks/ + settings.json-Registrierung Edit|Write bzw. Bash|PowerShell; allowlist.json-Einträge → permissions.allow; agents/release-readiness.md → .claude/agents/) + Phasen-Zuordnung (Phase-1/3/4/6/7-Zusätze) 1:1 aus Prompt B.
- [ ] **Step 3.2:** `HOMEY.md` ← VioletApp `HOMEY.md` 1:1, einzige Änderungen: Workflow-Retro-Datumsverweis raus („aus Workflow-Retro 2026-07-05, 3× denselben Bug getroffen" → „bewährte Regel: Smart-Quote-Delimiter brechen JSON"), Versions-Log-Beispielpfad bleibt `docs/dashboard/versions.md`.
- [ ] **Step 3.3:** Die 4 Hooks 1:1 aus VioletApp `.claude/hooks/` kopieren; Transformationen je Datei: Header-Spec-Pfade → `(extrahiert aus VioletApp — siehe Framework-CHANGELOG)`; in `secrets-guard.js` Wortlaut „Violet write credential/Violet write password" → „device write credential/device write password" (Regeln/Regexe UNVERÄNDERT, `VIOLET_WRITE_PASSWORD`-Env → `DEVICE_WRITE_PASSWORD` inkl. Kommentar); `check-version-sync.js`/`compose-guard.js`/`json-guard.js` sind bereits app-neutral (nur Header-Regel a). Telemetrie-`logHook`-Zeilen bleiben (lib/log.js kommt aus templates/).
- [ ] **Step 3.4:** `allowlist.json`:

```json
{
  "permissions.allow (Homey read-only)": [
    "Bash(homey whoami)",
    "Bash(homey api devices get-*)",
    "Bash(homey api flow get-*)",
    "Bash(homey api apps get-*)",
    "Bash(homey app validate *)",
    "Bash(npx homey app validate *)",
    "Bash(npx homey api devices get-*)"
  ]
}
```

- [ ] **Step 3.5:** `agents/release-readiness.md` ← VioletApp `.claude/agents/release-readiness.md`: „(Violet)" raus, Punkt 8 Live-Smoke als „projektspezifisches read-only Live-Skript (Muster: VioletApp scripts/live-smoke.js)" formuliert, Rest 1:1 (inkl. Triage-Inbox-Zeile, model/effort-Frontmatter).
- [ ] **Step 3.6:** Grep-Check `violet|Violet|de\.neunbft` über `homey/` → 0 Treffer. Commit: `git add homey && git commit -m "feat(homey): Plattform-Modul — HOMEY.md, 4 Guard-Hooks (generisch), Allowlist, release-readiness"`

---

### Task 4: VioletApp — Spiegelregel + Drift-Check + Settings-Repo-Pointer

**Files:**
- Modify: `VioletApp/CLAUDE.md` (Source-of-Truth-Abschnitt), `VioletApp/.claude/skills/milestone-checkpoint/SKILL.md`, `VioletApp/.claude/skills/dashboard-sync/SKILL.md`, `VioletApp/docs/dashboard/dashboard.html` (NUR →M5-`steps[]`)
- Modify: `skill-ClaudeCode-general-settings/prompts/agentic-loop-bootstrap.{de,en}.md` (+1 Notiz-Zeile), `skill-ClaudeCode-general-settings/README.md` (+Verweis)

- [ ] **Step 4.1:** VioletApp `CLAUDE.md`, im Abschnitt „Claude-Code-Settings: Skill = Source of Truth" nach der Faustregel anfügen:

```markdown
**Framework-Artefakte** (seit M4.9): Jede GENERISCHE Änderung an Gate-/Guard-Hooks,
CLAUDE.md-Protokollregeln, dem Checkpoint-Ablauf oder den Dashboard-/Template-Formaten wird
**in derselben Session** in das Framework-Repo `skill-agentic-loop-framework`
(`plugin/skills/agentic-loop-framework/templates/` bzw. `homey/`) gespiegelt + dessen
CHANGELOG ergänzt. Projekt-spezifisches (z. B. Violet-Live-Smoke, konkrete Allowlist-Einträge)
bleibt hier. Der milestone-checkpoint prüft Drift (Schritt 6).
```

- [ ] **Step 4.2:** milestone-checkpoint-SKILL.md: Schrittliste — nach Schritt 5 einfügen `6. **Framework-Drift prüfen** (siehe unten) — Projekt-Framework vs. skill-agentic-loop-framework abgleichen.`, alte 6→7 (Dashboard-Update, Zusammenfassung „Schritte 1–6"), alte 7→8 (Handover). Sektion `## Schritt 7: Handover (M4.8)` → `## Schritt 8: Handover (M4.8)`. Neue Sektion vor der Handover-Sektion:

```markdown
## Schritt 6: Framework-Drift prüfen (M4.9)

`git log --since=<letzter Checkpoint> --oneline -- .claude/hooks .claude/skills CLAUDE.md`
im Projekt sichten: Ist eine der Änderungen GENERISCH (in jedem Projekt sinnvoll)? Dann in
`C:/Users/TorstenSturm/source/repos/skill-agentic-loop-framework` die entsprechende Vorlage
(`templates/` bzw. `homey/`) nachziehen + CHANGELOG-Eintrag; Commit dort nach §9-Freigabe.
Kein Drift → kurz vermerken.
```

- [ ] **Step 4.3:** dashboard-sync-SKILL.md: „Sechs Steps" → „Sieben Steps, …, `Memory-Konsolidierung`, `Framework-Drift geprüft`, `Handover` (M4.8/M4.9)." — VioletApp `dashboard.html` →M5-`steps[]`: nach Memory-Konsolidierung `{ label: "Framework-Drift geprüft", done: false },` einfügen (7 Labels; →M6–→M8 macht Task 6). node-Eval-Parse.
- [ ] **Step 4.4:** Settings-Repo: in beiden `prompts/agentic-loop-bootstrap.*.md` nach der Titelzeile eine Notiz einfügen (`> SUPERSEDED (2026-07-09): Lebende Quelle ist jetzt der Skill im Repo skill-agentic-loop-framework (M4.9). Diese Prompts bleiben als historischer Seed.` / englisches Pendant in der en-Datei); `README.md` um einen Verweis-Absatz ergänzen.
- [ ] **Step 4.5:** VioletApp committen (`feat(m4.9): Spiegelregel auf Framework-Artefakte erweitert + checkpoint Schritt 6 Framework-Drift`); Settings-Repo committen (`docs: Bootstrap-Prompts superseded — Verweis auf skill-agentic-loop-framework`) — NICHT pushen (§9 am Ende). Danach Task-2-Step-2.7 ausführen (Checkpoint-Template extrahieren).

---

### Task 5: Trockentest (Spec D8)

- [ ] **Step 5.1:** `templates/` nach `$env:TEMP\alf-dryrun-<id>` kopieren (`Copy-Item -Recurse`).
- [ ] **Step 5.2:** Datenblock-Eval (erwartet `milestones: []` → `parse OK, milestones=0`), `JSON.parse` auf `.claude/settings.json`, dann im Temp-Baum `node --test test/hooks/test-gate.test.js` → Expected: 6 pass, 0 fail (der Baum ist durch package.json + Layout selbsttragend).
- [ ] **Step 5.3:** Ergebnisse im Chat zeigen; im Framework-Repo committen, falls Fixes nötig waren.

---

### Task 6: Prompt-Konsistenz-Sweep (VioletApp dashboard.html)

**Betroffen:** `prompt`-Felder von →M5, →M6, →M7, →M8, M5, M6, M7, M8, M9, M10 + `steps[]` von →M6, →M7, →M8.

- [ ] **Step 6.1:** Standard-Block (b)+(c) — in JEDEN der 10 Prompts direkt nach dem ersten Absatz (UMGEBUNG…) einfügen:

```
Startmodus (CLAUDE.md §10): autonomer Lauf per claude --permission-mode auto; Alltags-Session im Default-Modus (Allowlist greift). Lies zuerst docs/dashboard/triage-inbox.md (§7) — offene Findings vor neuer Arbeit sichten.
```

- [ ] **Step 6.2:** →M5/→M6/→M7/→M8-Prompts: vor der `/remote-control`-Zeile die Checkpoint-Checkliste einfügen:

```
DONE-BEDINGUNGEN (transcript-verifizierbar; Klartext-Fallback solange Claude Code < 2.1.139, danach als /goal-Zeile fuehrbar):
1. Alle Checkpoint-Schritte 1-8 durchlaufen (inkl. Memory-Konsolidierung als Diff, Framework-Drift-Check, Handover) — Ergebnisse im Chat zusammengefasst.
2. Der Checkpoint-Eintrag im Dashboard steht auf done (alle 7 Steps abgehakt) — Diff gezeigt.
```

- [ ] **Step 6.3:** →M6/→M7/→M8-`steps[]` auf die 7 Labels setzen (wie →M5 nach Task 4): `/fewer-permission-prompts`, `/claude-automation-recommender`, `Skill-Quellen geprüft`, `Workflow-Retrospektive`, `Memory-Konsolidierung`, `Framework-Drift geprüft`, `Handover`.
- [ ] **Step 6.4:** Veraltete Referenzen (d): M5-Prompt-Gates umformulieren — `(a) Mock-Harness test/mocks/homey.js existiert und deckt das device.js-Basisverhalten ab (SEIT M4.7 VORHANDEN — 8 Reconcile-Tests);` / `(b) GitHub-Actions-CI laeuft gruen (SEIT M4.6 VORHANDEN — aktuellen Lauf zeigen);` / `(c)` unverändert. M6-Prompt: Hinweis ergänzen, dass der Dev-Gate-validate inzwischen von test-gate/stop-verify/CI flankiert wird — Satz: `Die M4.6-M4.8-Gates (test-gate, stop-verify, CI, release-gate) sind aktiv.`; gleiche Zeile in M7/M8/M9/M10 NICHT nötig (Prompts verweisen auf CLAUDE.md-Protokolle, die das abdecken — nur prüfen, dass nichts Falsches drinsteht).
- [ ] **Step 6.5:** node-Eval-Parse + `git diff docs/dashboard/dashboard.html` im Chat zeigen (Sweep-Diff). Commit: `docs(dashboard): M4.9 Prompt-Konsistenz-Sweep — Startmodus+Triage-Zeile, Checkpoint-Checklisten+7-Steps, M5-Gate-Referenzen aktualisiert`.

---

### Task 7: Abnahme (Spec §3) + Dashboard + /code-review BEIDE Repos + §9

- [ ] **Step 7.1:** /goal-Nachweise im Chat bündeln: Trockentest-Outputs (5.2), Framework-Repo `git log --oneline`, VioletApp-CLAUDE.md-Diff + checkpoint-Diff, Sweep-Diff + Eval.
- [ ] **Step 7.2:** Dashboard M4.9: Steps Spec/Plan/Implementierung done, Logs (Entscheid a, Trockentest, Sweep), currentActivity Review/Push; Commit.
- [ ] **Step 7.3:** `/code-review` auf (1) VioletApp-Branch-Diff, (2) Framework-Repo-Stand, (3) Settings-Repo-Diff; Funde fixen.
- [ ] **Step 7.4:** §9-Frage für alle drei Repos (VioletApp: direkt/PR · Framework: push main · Settings: push) — auf explizites Ja warten. Danach: CI grün (VioletApp), Dashboard M4.9 done (prompt: null), Memory, →M5-Checkpoint als nächster Schritt ankündigen.
