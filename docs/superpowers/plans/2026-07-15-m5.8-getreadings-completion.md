# M5.8 getReadings-Vervollständigung — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Config-gesteuerte Messeingangs-Kacheln (`measure_adc.<n>`, `measure_impulse.<n>`) plus Parser-Fixes (Range h-Suffix/`>`-Präfix, STATE Array+Pipe-String) und STATE-Diagnose-Annotation — Spec: `docs/superpowers/specs/2026-07-15-violet-homey-app-m5.8-getreadings-completion-design.md`.

**Architecture:** Alle Logik pur in `lib/FeatureGroups.js` + `lib/ConfigSource.js` (TDD via node:test); `drivers/pool/device.js` bleibt dünne Verdrahtung (Reconcile + `setCapabilityOptions` mit Churn-Guard). Gating ausschließlich über ConfigFacts (`use=1`), Kanal-Metadaten (Name/Einheit/Dezimalstellen) aus getConfig.

**Tech Stack:** Homey SDK v3 (Compose), Node 18+, node:test, keine neuen Dependencies (Dependency-Hygiene: nichts zu belegen).

## Global Constraints

- Spec-Referenzen in Datei-Headern/Kommentaren: `§N` = M5.8-Spec (documenting-code-Skill anwenden).
- JSON-Dateien nie von Hand tippen — per `node -e "…JSON.stringify…"` erzeugen/ändern oder exakt aus diesem Plan kopieren (json-guard-Hook).
- Jede Feld-Übernahme ist live belegt (Spec §1) — KEINE weiteren getReadings/getConfig-Felder hinzufügen.
- Keine Alarme in M5.8 (User-Entscheidung, Spec §2 Non-Goals).
- `CONFIG_QUERY` bleibt whitelist-only (SR-11): nur die in Task 3 genannten Keys ergänzen, keine Secret-Gruppen.
- Dev-Gate pro Task: `npm test` fail 0; am Ende zusätzlich `npx homey app validate --level=debug` PASS.
- Commits auf Branch `m5.8-getreadings-completion`; Trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- app.json ist generiert — nach Compose-Änderungen (Task 5) via validate regenerieren und MIT committen (Version == .homeycompose/app.json).

---

### Task 1: `parseRangeToDays` — h-Suffix + `>`-Präfix (Spec §5)

**Files:**
- Modify: `lib/FeatureGroups.js:36-46` (Funktion `parseRangeToDays`)
- Test: `test/FeatureGroups.test.js`

**Interfaces:**
- Produces: `parseRangeToDays(str) -> number|null` — neu: `"33h"` → 1.38 (h÷24, 2 Nachkommastellen), `">99d"` → 99; d/w/m unverändert.

- [ ] **Step 1: Failing Tests schreiben** — in `test/FeatureGroups.test.js` neben den bestehenden `parseRangeToDays`-Tests ergänzen:

```js
test('parseRangeToDays: h-Suffix wird in Tage umgerechnet (Spec §5, live "33h")', () => {
  assert.strictEqual(parseRangeToDays('33h'), 1.38); // 33/24, 2 Nachkommastellen
  assert.strictEqual(parseRangeToDays('24h'), 1);
});

test('parseRangeToDays: ">"-Praefix wird akzeptiert (Spec §5, live ">99d")', () => {
  assert.strictEqual(parseRangeToDays('>99d'), 99);
  assert.strictEqual(parseRangeToDays('> 99d'), 99);
});

test('parseRangeToDays: bestehende d/w/m-Semantik unveraendert', () => {
  assert.strictEqual(parseRangeToDays('37d'), 37);
  assert.strictEqual(parseRangeToDays('6w'), 42);
  assert.strictEqual(parseRangeToDays('2m'), 60);
  assert.strictEqual(parseRangeToDays('kaputt'), null);
});
```

- [ ] **Step 2: Fail verifizieren** — Run: `node --test test/FeatureGroups.test.js` → die zwei neuen Tests FAIL (`'33h'` liefert null, `'>99d'` liefert null).

- [ ] **Step 3: Implementierung** — `parseRangeToDays` ersetzen (Header-Kommentar/§-Ref mitziehen):

```js
/**
 * Parse a Violet range string ("37d"/"6w"/"2m"/"33h"/">99d") to days
 * (M2 spec §5, §9; M5.8 spec §5 — h-Suffix und ">"-Präfix live belegt).
 * @param {*} str e.g. "33h" (coerced via String(); raw fields may be non-string).
 * @returns {?number} Days (w=×7, m=×30, h=÷24 auf 2 Nachkommastellen), or null if unparseable.
 */
function parseRangeToDays(str) {
  const m = /^\s*>?\s*(\d+(?:\.\d+)?)\s*([dwmh])\s*$/i.exec(String(str ?? ''));
  if (!m) return null;
  const unit = m[2].toLowerCase();
  if (unit === 'h') return Math.round((Number(m[1]) / 24) * 100) / 100;
  const mult = (/** @type {Object<string, number>} */ ({ d: 1, w: 7, m: 30 }))[unit];
  return Number(m[1]) * mult;
}
```

- [ ] **Step 4: Pass verifizieren** — Run: `node --test test/FeatureGroups.test.js` → alle PASS (der `{ todo: true }`-Test bleibt todo). Dann `npm test` → fail 0.

- [ ] **Step 5: Commit**

```bash
git add lib/FeatureGroups.js test/FeatureGroups.test.js
git commit -m "fix(m5.8): parseRangeToDays versteht h-Suffix und >-Praefix (stale CL-Kachel behoben)"
```

---

### Task 2: STATE-Parser `stateReasons`/`stateBlocked` + `alarm_dosing_blocked`-Fix (Spec §5)

**Files:**
- Modify: `lib/FeatureGroups.js` (Funktion `faultQueueActive` ersetzen, Zeilen 57-64; Verwendung Zeile 196; Exporte Zeile 270-284)
- Modify: `drivers/pool/device.js` (Import-Destructure Zeile 22-31; Trigger-Token Zeile 390-392)
- Test: `test/FeatureGroups.test.js` (Import Zeile 9; alter `faultQueueActive`-Test Zeilen 71-75; todo-Test Zeilen 171-181)

**Interfaces:**
- Produces: `stateReasons(v) -> string[]` (Array → Elemente; Pipe-String `"0|GRUND"` → `["GRUND"]`; sonst `[]`), `stateBlocked(v) -> boolean` (ein Grund beginnt mit `BLOCKED_BY_`). `faultQueueActive` ENTFÄLLT (Export + Tests ersetzen).
- Consumes: —

- [ ] **Step 1: Failing Tests schreiben** — in `test/FeatureGroups.test.js`: Import Zeile 9 `faultQueueActive,` durch `stateReasons,\n  stateBlocked,` ersetzen; den Test „faultQueueActive is true for a non-empty array only" (Zeilen 71-75) ERSETZEN durch:

```js
test('stateReasons: Array-, Pipe-String- und Skalar-Formate (Spec §5, live belegt)', () => {
  assert.deepStrictEqual(stateReasons(['BLOCKED_BY_MAX_AMOUNT']), ['BLOCKED_BY_MAX_AMOUNT']);
  assert.deepStrictEqual(stateReasons([]), []);
  assert.deepStrictEqual(stateReasons('0|BLOCKED_BY_SENSOR_FAULT'), ['BLOCKED_BY_SENSOR_FAULT']);
  assert.deepStrictEqual(stateReasons('0'), []);
  assert.deepStrictEqual(stateReasons(0), []);
  assert.deepStrictEqual(stateReasons(undefined), []);
});

test('stateBlocked: nur BLOCKED_BY_* gilt als Block (Spec §5; Notiz §2-Vokabular)', () => {
  assert.strictEqual(stateBlocked(['BLOCKED_BY_MAX_AMOUNT']), true);
  assert.strictEqual(stateBlocked('0|BLOCKED_BY_SENSOR_FAULT'), true);
  assert.strictEqual(stateBlocked(['CL_DOSING_CONTROLLER']), false);
  assert.strictEqual(stateBlocked(['TRESHOLDS_REACHED']), false);
  assert.strictEqual(stateBlocked(['MANUAL_DOSING']), false);
  assert.strictEqual(stateBlocked([]), false);
  assert.strictEqual(stateBlocked('OK'), false);
  assert.strictEqual(stateBlocked(undefined), false);
});
```

Zusätzlich beim eingefrorenen Test (Zeile 177) das `{ todo: true }` entfernen und den Kommentar davor (Zeilen 171-176) ersetzen durch:

```js
// M5.8 (Spec §5): entfroren — stateBlocked wertet nur BLOCKED_BY_* als Block;
// CL_DOSING_CONTROLLER ist Normalbetrieb (live-verifiziert, versions.md 0.3.1).
```

- [ ] **Step 2: Fail verifizieren** — Run: `node --test test/FeatureGroups.test.js` → FAIL (stateReasons/stateBlocked nicht exportiert; Ex-todo-Test schlägt fehl).

- [ ] **Step 3: Implementierung** — in `lib/FeatureGroups.js` die Funktion `faultQueueActive` (Zeilen 57-64) ERSETZEN durch:

```js
/**
 * Fehl-/Blockiergründe eines Violet-*STATE-Felds (M5.8 spec §5). Zwei live
 * belegte Formate: Fault-Queue-Array (["GRUND", …]) und Pipe-String
 * "<wert>|<grund>[|<grund>…]" (z. B. SOLARSTATE="0|BLOCKED_BY_SENSOR_FAULT").
 * Skalare ohne Pipe ("0", "OK", Zahlen) tragen keine Gründe.
 * @param {*} v Raw field value.
 * @returns {string[]} Gründe (ggf. leer).
 */
function stateReasons(v) {
  if (Array.isArray(v)) return v.map((x) => String(x));
  const s = String(v ?? '');
  if (!s.includes('|')) return [];
  return s.split('|').slice(1).filter((r) => r !== '');
}

/**
 * True when a *STATE field carries a real block (M5.8 spec §5): nur das
 * live/forum-belegte Vokabular BLOCKED_BY_* zählt; CL_DOSING_CONTROLLER,
 * MANUAL_*, TRESHOLDS_REACHED* sind Normalbetrieb (M4.7-todo-Test-Fix).
 * @param {*} v Raw field value.
 * @returns {boolean}
 */
function stateBlocked(v) {
  return stateReasons(v).some((r) => r.startsWith('BLOCKED_BY_'));
}
```

Zeile 196 ändern zu:

```js
    if (raw[`${p}_STATE`] !== undefined) u[`alarm_dosing_blocked.${ch}`] = stateBlocked(raw[`${p}_STATE`]);
```

Im `module.exports`-Block `faultQueueActive,` durch `stateReasons,\n  stateBlocked,` ersetzen.

In `drivers/pool/device.js`: im FeatureGroups-Destructure (Zeilen 22-31) `stateReasons,` ergänzen; Zeilen 390-392 (Trigger-Token) ändern von

```js
        const stateVal = raw[`${dosingChannelPrefix(ch)}_STATE`];
        const reason = Array.isArray(stateVal) ? stateVal.join(', ') : '';
```

zu

```js
        const stateVal = raw[`${dosingChannelPrefix(ch)}_STATE`];
        const reason = stateReasons(stateVal).join(', ');
```

- [ ] **Step 4: Pass verifizieren** — Run: `npm test` → **todo 0**, fail 0 (der Ex-todo-Test läuft jetzt grün mit).

- [ ] **Step 5: Commit**

```bash
git add lib/FeatureGroups.js drivers/pool/device.js test/FeatureGroups.test.js
git commit -m "fix(m5.8): STATE-Parser fuer Array+Pipe-String, nur BLOCKED_BY_* blockiert — todo-Test entfroren"
```

---

### Task 3: ConfigSource — Whitelist-Delta + Kanal-Metadaten (Spec §4)

**Files:**
- Modify: `lib/ConfigSource.js` (CONFIG_QUERY Zeilen 12-21; ConfigFacts-typedef Zeilen 23-40; parseConfigFacts Zeilen 77-93)
- Modify: `lib/FeatureDetector.js` (Features-typedef Zeilen 20-21)
- Modify: `test/fixtures/getconfig-reference.json` (per node-Skript)
- Test: `test/ConfigSource.test.js`

**Interfaces:**
- Produces: `ConfigFacts.adcChannels: Array<{id, use, units, name, decimals: ?number}>`, `ConfigFacts.impulsChannels: Array<{id, use, units, name: string}>`; `CONFIG_QUERY` enthält zusätzlich `NAMES_impulscount` (Präfix) und `ANALOG_adc1_decimal`…`ANALOG_adc6_decimal`.
- Consumes: `flag()`, `label()` (bestehende interne Helper).

- [ ] **Step 1: Fixture erweitern** (json-guard: per node-Skript, nicht von Hand):

```bash
node -e "const fs=require('fs');const p='test/fixtures/getconfig-reference.json';const j=JSON.parse(fs.readFileSync(p,'utf8'));Object.assign(j,{ANALOG_adc1_decimal:'2',NAMES_impulscount1:'Anströmung',NAMES_impulscount2:'Förderleistung'});fs.writeFileSync(p,JSON.stringify(j,null,2)+'\n')"
```

(Nur adc1 bekommt einen decimal-Key — testet zugleich den Fallback `null` für adc2..6.)

- [ ] **Step 2: Failing Tests schreiben** — in `test/ConfigSource.test.js` ergänzen (bestehende Import-/Fixture-Muster der Datei verwenden; das Referenz-Fixture wird dort bereits geparst):

```js
test('M5.8 §4: adcChannels tragen decimals (Key vorhanden → Zahl, sonst null)', () => {
  const facts = parseConfigFacts(referenceConfig);
  const adc1 = facts.adcChannels.find((c) => c.id === 1);
  const adc2 = facts.adcChannels.find((c) => c.id === 2);
  assert.strictEqual(adc1.decimals, 2);
  assert.strictEqual(adc2.decimals, null);
});

test('M5.8 §4: impulsChannels tragen den NAMES_impulscount-Namen', () => {
  const facts = parseConfigFacts(referenceConfig);
  assert.strictEqual(facts.impulsChannels.find((c) => c.id === 1).name, 'Anströmung');
  assert.strictEqual(facts.impulsChannels.find((c) => c.id === 2).name, 'Förderleistung');
});

test('M5.8 §4: CONFIG_QUERY enthaelt die neuen Keys und weiter keine Secret-Gruppen', () => {
  assert.ok(CONFIG_QUERY.includes('NAMES_impulscount'));
  for (let i = 1; i <= 6; i += 1) assert.ok(CONFIG_QUERY.includes(`ANALOG_adc${i}_decimal`));
  for (const k of CONFIG_QUERY) assert.ok(!/^(NET_|NOTIFY_|USER_|AUTH_|BACKUP_|SERVICES_)/.test(k), k);
});
```

Hinweis: Falls die Testdatei `referenceConfig` bzw. `CONFIG_QUERY` noch nicht importiert, Import-Zeilen entsprechend dem Kopf von `test/drivers/pool.device.config.test.js` ergänzen (`parseConfigFacts`, `CONFIG_QUERY` aus `../lib/ConfigSource`; Fixture via `fs.readFileSync('test/fixtures/getconfig-reference.json')` + `JSON.parse` — hier das ROH-Objekt an `parseConfigFacts` geben, nicht das bereits geparste Facts-Objekt).

- [ ] **Step 3: Fail verifizieren** — Run: `node --test test/ConfigSource.test.js` → FAIL (decimals/name undefined, CONFIG_QUERY-Keys fehlen).

- [ ] **Step 4: Implementierung** — in `lib/ConfigSource.js`:

`CONFIG_QUERY` (nach der `NAMES_adc`-Zeile) ergänzen:

```js
  'NAMES_impulscount', // Präfix — expandiert zu NAMES_impulscount1/2 (M5.8 Spec §4, live belegt)
  'ANALOG_adc1_decimal', 'ANALOG_adc2_decimal', 'ANALOG_adc3_decimal', 'ANALOG_adc4_decimal', 'ANALOG_adc5_decimal', 'ANALOG_adc6_decimal',
```

typedef-Zeilen anpassen:

```js
 * @property {Array<{id: number, use: boolean, units: string, name: string, decimals: ?number}>} adcChannels
 * @property {Array<{id: number, use: boolean, units: string, name: string}>} impulsChannels
```

In `parseConfigFacts` den adc-Push erweitern und den impuls-Push ersetzen:

```js
    const dec = Number(raw[`ANALOG_adc${id}_decimal`]);
    adcChannels.push({
      id,
      use,
      units: String(raw[`ANALOG_adc${id}_units`] ?? ''),
      name: label(raw[`NAMES_adc${id}`]) ?? '',
      decimals: Number.isFinite(dec) ? dec : null, // M5.8 Spec §4: GUI-Nachkommastellen
    });
```

```js
    impulsChannels.push({
      id,
      use,
      units: String(raw[`IMPULS_input${id}_units`] ?? ''),
      name: label(raw[`NAMES_impulscount${id}`]) ?? '', // M5.8 Spec §4: NAMES_impulscountN (live belegt)
    });
```

In `lib/FeatureDetector.js` die typedef-Zeilen 20-21 an die neuen Shapes angleichen (`decimals: ?number` bei adc, `name: string` bei impuls).

- [ ] **Step 5: Pass verifizieren** — Run: `npm test` → fail 0 (auch M5.7-Bestandstests grün — neue Felder sind additiv).

- [ ] **Step 6: Commit**

```bash
git add lib/ConfigSource.js lib/FeatureDetector.js test/ConfigSource.test.js test/fixtures/getconfig-reference.json
git commit -m "feat(m5.8): ConfigFacts um Impuls-Namen (NAMES_impulscount) und ADC-Dezimalstellen erweitert"
```

---

### Task 4: Gating + Poll-Werte für Messeingänge (Spec §3)

**Files:**
- Modify: `lib/FeatureGroups.js` (`desiredM2Capabilities` Zeilen 112-138; `buildM2Updates` Zeilen 149-199; Exporte)
- Test: `test/FeatureGroups.test.js`

**Interfaces:**
- Consumes: `features.adcChannels` / `features.impulsChannels` (aus Task 3 / M5.7).
- Produces: `INPUT_SUBCAPS = ['measure_adc', 'measure_impulse']` (Export, für die Managed-Base-Menge in device.js); `desiredM2Capabilities` liefert zusätzlich `measure_adc.<id>`/`measure_impulse.<id>` (`overrides.inputs`: auto=use-Kanäle, force=alle gemeldeten, hide=keine); `buildM2Updates` liefert `measure_adc.<id>`/`measure_impulse.<id>` = numerischer Rohwert.

- [ ] **Step 1: Failing Tests schreiben** — in `test/FeatureGroups.test.js`:

```js
test('M5.8 §3: desiredM2Capabilities gated Messeingaenge auf use=1', () => {
  const features = { pump: true, dosingChannels: [],
    adcChannels: [
      { id: 1, use: true, units: 'Bar', name: 'Filterdruck', decimals: 2 },
      { id: 2, use: false, units: 'cm', name: 'Schwallwasser', decimals: null },
    ],
    impulsChannels: [
      { id: 1, use: true, units: 'cm/s', name: 'Anströmung' },
      { id: 2, use: false, units: 'm³/h', name: 'Förderleistung' },
    ] };
  const caps = desiredM2Capabilities({ features, overrides: {}, diagnosticsEnabled: false });
  assert.ok(caps.includes('measure_adc.1'));
  assert.ok(!caps.includes('measure_adc.2'));
  assert.ok(caps.includes('measure_impulse.1'));
  assert.ok(!caps.includes('measure_impulse.2'));
});

test('M5.8 §3: inputs-Override force zeigt auch use=0, hide entfernt alle', () => {
  const features = { pump: true, dosingChannels: [],
    adcChannels: [{ id: 2, use: false, units: 'cm', name: '', decimals: null }],
    impulsChannels: [{ id: 1, use: true, units: 'cm/s', name: '' }] };
  const forced = desiredM2Capabilities({ features, overrides: { inputs: 'force' }, diagnosticsEnabled: false });
  assert.ok(forced.includes('measure_adc.2'));
  const hidden = desiredM2Capabilities({ features, overrides: { inputs: 'hide' }, diagnosticsEnabled: false });
  assert.ok(!hidden.some((c) => c.startsWith('measure_adc.') || c.startsWith('measure_impulse.')));
});

test('M5.8 §3: ohne ConfigFacts (leere Kanallisten) keine Eingangs-Kacheln', () => {
  const caps = desiredM2Capabilities({ features: { pump: true, dosingChannels: [], adcChannels: [], impulsChannels: [] }, overrides: {}, diagnosticsEnabled: false });
  assert.ok(!caps.some((c) => c.startsWith('measure_adc.') || c.startsWith('measure_impulse.')));
});

test('M5.8 §3: buildM2Updates liefert ADC/IMP-Rohwerte (Pumpe-AN-Fixture: 0.48 Bar / 10.2)', () => {
  const raw = require('./fixtures/getReadings.all.json');
  const u = buildM2Updates(raw, { dosingChannels: [] });
  assert.strictEqual(u['measure_adc.1'], 0.48);
  assert.strictEqual(u['measure_impulse.1'], 10.2);
});

test('M5.8 §3/§7: Pumpe-AUS-Werte kommen roh durch, fehlende Felder fehlen', () => {
  const u = buildM2Updates({ ADC1_value: -0.11, IMP1_value: 0 }, { dosingChannels: [] });
  assert.strictEqual(u['measure_adc.1'], -0.11);
  assert.strictEqual(u['measure_impulse.1'], 0);
  assert.ok(!('measure_adc.2' in u));
  assert.ok(!('measure_impulse.2' in u));
});
```

- [ ] **Step 2: Fail verifizieren** — Run: `node --test test/FeatureGroups.test.js` → die neuen Tests FAIL.

- [ ] **Step 3: Implementierung** — in `lib/FeatureGroups.js`:

Nach `DIAGNOSTIC_CAPS` (Zeile 110) ergänzen:

```js
// M5.8 (Spec §3): Basen der Messeingangs-Sub-Caps — device.js nimmt sie in die
// Managed-Base-Menge auf, damit Remove-Reconcile sie nie übersieht.
const INPUT_SUBCAPS = ['measure_adc', 'measure_impulse'];
```

In `desiredM2Capabilities` vor `// Diagnostics: …` einfügen (JSDoc der Funktion um den `inputs`-Override-Satz ergänzen):

```js
  // Messeingänge (M5.8 Spec §3): auto = use=1-Kanäle (Config-autoritativ in
  // beide Richtungen, wie Cover M5.7 §6); force = alle gemeldeten Kanäle
  // (bewusste User-Entscheidung inkl. Müllwerten); hide = keine.
  const inputsMode = overrides.inputs || 'auto';
  if (inputsMode !== 'hide') {
    for (const ch of (features.adcChannels || [])) {
      if (inputsMode === 'force' || ch.use) caps.push(`measure_adc.${ch.id}`);
    }
    for (const ch of (features.impulsChannels || [])) {
      if (inputsMode === 'force' || ch.use) caps.push(`measure_impulse.${ch.id}`);
    }
  }
```

In `buildM2Updates` vor dem Dosing-Loop einfügen:

```js
  // Messeingänge (M5.8 Spec §3): rohe Kanalwerte; welche Kacheln existieren,
  // entscheidet der Reconcile (use-Gating) — device.js setzt nur vorhandene Caps.
  for (let id = 1; id <= 6; id += 1) {
    const v = num(raw[`ADC${id}_value`]);
    if (v !== null) u[`measure_adc.${id}`] = v;
  }
  for (let id = 1; id <= 2; id += 1) {
    const v = num(raw[`IMP${id}_value`]);
    if (v !== null) u[`measure_impulse.${id}`] = v;
  }
```

`INPUT_SUBCAPS` in `module.exports` aufnehmen.

- [ ] **Step 4: Pass verifizieren** — Run: `npm test` → fail 0.

- [ ] **Step 5: Commit**

```bash
git add lib/FeatureGroups.js test/FeatureGroups.test.js
git commit -m "feat(m5.8): Messeingangs-Gating (inputs-Gruppe) + ADC/IMP-Rohwerte in buildM2Updates"
```

---

### Task 5: Capability-Definitionen, Settings-Radio, Device-Verdrahtung (Spec §3)

**Files:**
- Create: `.homeycompose/capabilities/measure_adc.json`, `.homeycompose/capabilities/measure_impulse.json`
- Modify: `drivers/pool/driver.settings.compose.json` (nach `group_dosing`, Zeile 167-168)
- Modify: `drivers/pool/device.js` (Import Zeilen 22-31; Klassenfeld bei Zeile 76; `_reconcileCapabilities` Zeilen 460-495; `onSettings` Zeile 277)
- Test: `test/drivers/pool.device.config.test.js`

**Interfaces:**
- Consumes: `INPUT_SUBCAPS`, `desiredM2Capabilities` (Task 4), `features.adcChannels`/`impulsChannels` mit `name`/`units`/`decimals` (Task 3).
- Produces: Kacheln `measure_adc.<id>`/`measure_impulse.<id>` mit Titel/Einheit/Dezimalstellen aus der Config; Setting `group_inputs`.

- [ ] **Step 1: Capability-JSONs anlegen** (exakt diese Inhalte; Muster = `measure_dosing_daily_ml.json`):

`.homeycompose/capabilities/measure_adc.json`:

```json
{
  "type": "number",
  "title": { "en": "Analog input", "de": "Analogeingang" },
  "uiComponent": "sensor",
  "getable": true,
  "setable": false,
  "insights": true,
  "decimals": 2
}
```

`.homeycompose/capabilities/measure_impulse.json`:

```json
{
  "type": "number",
  "title": { "en": "Pulse input", "de": "Impulseingang" },
  "uiComponent": "sensor",
  "getable": true,
  "setable": false,
  "insights": true,
  "decimals": 1
}
```

(Keine festen `units` im Manifest — sie kommen pro Instanz aus der Config via `setCapabilityOptions`.)

- [ ] **Step 2: Settings-Radio ergänzen** — in `drivers/pool/driver.settings.compose.json` direkt NACH dem `group_dosing`-Objekt (Zeile 168) einfügen:

```json
      { "id": "group_inputs", "type": "radio", "label": { "en": "Measurement inputs (analog/pulse)", "de": "Messeingänge (Analog/Impuls)" }, "value": "auto",
        "hint": { "en": "Auto shows the inputs enabled in the Violet configuration, with their configured name and unit (e.g. filter pressure in bar). Always show also lists disabled inputs — their raw values may be meaningless. Hide removes all input tiles.", "de": "Auto zeigt die in der Violet-Konfiguration aktivierten Eingänge mit konfiguriertem Namen und Einheit (z. B. Filterdruck in Bar). Immer anzeigen listet auch deaktivierte Eingänge — deren Rohwerte können bedeutungslos sein. Ausblenden entfernt alle Eingangs-Kacheln." },
        "values": [ { "id": "auto", "label": { "en": "Auto (if enabled in Violet)", "de": "Auto (wenn in Violet aktiviert)" } }, { "id": "force", "label": { "en": "Always show", "de": "Immer anzeigen" } }, { "id": "hide", "label": { "en": "Hide", "de": "Ausblenden" } } ] },
```

- [ ] **Step 3: Failing Device-Tests schreiben** — in `test/drivers/pool.device.config.test.js` (nutzt `makeDevice` + `referenceConfig`; Fixture-Config meldet adc1/2/3 + imp1 mit use=1, adc4/5/6 + imp2 use=0; adc1 hat decimals=2, Name „Filterdruck", Einheit „Bar"):

```js
test('M5.8 §3: use=1-Eingaenge werden Caps mit Titel/Einheit/Dezimalstellen aus der Config', async () => {
  configResult = referenceConfig;
  const device = await makeDevice({ ADC1_value: 0.48, IMP1_value: 10.2 });
  assert.ok(device.hasCapability('measure_adc.1'));
  assert.ok(device.hasCapability('measure_impulse.1'));
  assert.ok(!device.hasCapability('measure_adc.4'));
  assert.ok(!device.hasCapability('measure_impulse.2'));
  const opts = device.__test.capabilityOptions['measure_adc.1'];
  assert.strictEqual(opts.title.de, 'Filterdruck');
  assert.strictEqual(opts.units, 'Bar');
  assert.strictEqual(opts.decimals, 2);
  assert.strictEqual(device.__test.capabilities['measure_adc.1'], 0.48);
  assert.strictEqual(device.__test.capabilities['measure_impulse.1'], 10.2);
});

test('M5.8 §3: group_inputs=hide entfernt alle Eingangs-Kacheln', async () => {
  configResult = referenceConfig;
  const device = await makeDevice({ ADC1_value: 0.48 }, { group_inputs: 'hide' });
  assert.ok(!device.getCapabilities().some((c) => c.startsWith('measure_adc.') || c.startsWith('measure_impulse.')));
});

test('M5.8 §3: Fallback-Titel ohne NAMES-Label', async () => {
  const noNames = { ...referenceConfig, adcChannels: referenceConfig.adcChannels.map((c) => ({ ...c, name: '' })) };
  configResult = noNames;
  const device = await makeDevice({ ADC1_value: 0.48 });
  assert.strictEqual(device.__test.capabilityOptions['measure_adc.1'].title.de, 'Analogeingang 1');
});
```

(Property-Namen `__test.capabilityOptions` / `__test.capabilities` an die real vom Mock aufgezeichneten Namen anpassen — Bestandstests in derselben Datei zeigen das Muster; NICHT den Mock umbauen, falls er die Options bereits aufzeichnet.)

- [ ] **Step 4: Fail verifizieren** — Run: `node --test test/drivers/pool.device.config.test.js` → neue Tests FAIL (Caps fehlen).

- [ ] **Step 5: Device-Verdrahtung** — in `drivers/pool/device.js`:

1. Import-Destructure um `INPUT_SUBCAPS,` ergänzen.
2. Bei den Klassenfeldern (neben `_owTitleState = {}`, Zeile 76) ergänzen:

```js
  // M5.8 (Spec §3): letzter Options-Stand je Eingangs-Kachel (Churn-Guard,
  // Muster _owTitleState) — setCapabilityOptions nur bei echter Änderung.
  /** @type {Object<string, string>} */
  _inputOptState = {};
```

3. In `_reconcileCapabilities` (Block 3, Zeile 462-464) den Override ergänzen:

```js
    m2Overrides.inputs = this.getSetting('group_inputs') || undefined;
```

4. `M2_MANAGED_BASES` (Zeile 485-489) um die Eingangs-Basen erweitern:

```js
    const M2_MANAGED_BASES = new Set([
      ...Object.values(M2_GROUPS).flatMap((g) => g.capIds),
      ...DOSING_SUBCAPS,
      ...DIAGNOSTIC_CAPS,
      ...INPUT_SUBCAPS,
    ]);
```

5. Direkt nach der Add-Schleife (`for (const cap of desiredM2) { … }`, nach Zeile 481) den Options-Block einfügen:

```js
    // M5.8 (Spec §3): Eingangs-Kacheln tragen Titel/Einheit/Dezimalstellen aus
    // der Violet-Config (NAMES_*, *_units, *_decimal); Fallback-Titel generisch,
    // Fallback-Dezimalstellen = Manifest-Default (adc 2, impuls 1).
    const inputChannels = /** @type {Array<[string, Array<*>]>} */ ([
      ['measure_adc', (features.adcChannels || [])],
      ['measure_impulse', (features.impulsChannels || [])],
    ]);
    for (const [base, chans] of inputChannels) {
      for (const ch of chans) {
        const cap = `${base}.${ch.id}`;
        if (!this.hasCapability(cap)) continue;
        const fallback = base === 'measure_adc'
          ? { en: `Analog input ${ch.id}`, de: `Analogeingang ${ch.id}` }
          : { en: `Pulse input ${ch.id}`, de: `Impulseingang ${ch.id}` };
        const title = ch.name ? { en: ch.name, de: ch.name } : fallback;
        const decimals = (typeof ch.decimals === 'number') ? ch.decimals : (base === 'measure_adc' ? 2 : 1);
        const key = `${title.en}|${ch.units}|${decimals}`;
        if (this._inputOptState[cap] === key) continue;
        await this.setCapabilityOptions(cap, { title, units: ch.units, decimals }).catch(this.error);
        this._inputOptState[cap] = key;
      }
    }
```

6. In `onSettings` (Zeile 277) `group_inputs` in die Prompt-Re-Tick-Liste aufnehmen:

```js
    if (changedKeys.includes('control_enabled') || changedKeys.includes('show_advanced_diagnostics') || changedKeys.includes('group_inputs')) this._tick().catch(this.error);
```

- [ ] **Step 6: Pass verifizieren** — Run: `npm test` → fail 0. Dann `npx homey app validate --level=debug` → PASS (regeneriert `app.json` — Diff prüfen: nur die zwei neuen Capabilities + Setting).

- [ ] **Step 7: Commit**

```bash
git add .homeycompose/capabilities/measure_adc.json .homeycompose/capabilities/measure_impulse.json drivers/pool/driver.settings.compose.json drivers/pool/device.js test/drivers/pool.device.config.test.js app.json
git commit -m "feat(m5.8): Messeingangs-Kacheln measure_adc/measure_impulse mit Config-Titel/Einheit/Dezimalstellen"
```

---

### Task 6: Diagnose-Annotation mit STATE-Gründen (Spec §6)

**Files:**
- Modify: `lib/FeatureGroups.js` (`DIAG_SIMPLE`-Bereich Zeilen 207-228; `diagRawValue` Zeilen 251-268)
- Test: `test/FeatureGroups.test.js`

**Interfaces:**
- Consumes: `stateReasons` (Task 2).
- Produces: `diagRawValue` hängt für pump_running/heater_active/solar_active die STATE-Gründe an (`"2 | BLOCKED_BY_SENSOR_FAULT"`); `diagAnnotatable` unverändert.

- [ ] **Step 1: Failing Tests schreiben**:

```js
test('M5.8 §6: diagRawValue haengt STATE-Gruende an (Pipe-String live belegt)', () => {
  const raw = { SOLAR: 2, SOLARSTATE: '0|BLOCKED_BY_SENSOR_FAULT', PUMP: 1, PUMPSTATE: [], HEATER: 0, HEATERSTATE: ['BLOCKED_BY_ESC'] };
  assert.strictEqual(diagRawValue('solar_active', raw), '2 | BLOCKED_BY_SENSOR_FAULT');
  assert.strictEqual(diagRawValue('pump_running', raw), '1');
  assert.strictEqual(diagRawValue('heater_active', raw), '0 | BLOCKED_BY_ESC');
});
```

(Import `diagRawValue` existiert in der Testdatei bereits — sonst ergänzen.)

- [ ] **Step 2: Fail verifizieren** — Run: `node --test test/FeatureGroups.test.js` → neuer Test FAIL (`'2'` statt `'2 | …'`).

- [ ] **Step 3: Implementierung** — in `lib/FeatureGroups.js` nach `DIAG_DOSING` (Zeile 228) ergänzen:

```js
// M5.8 (Spec §6): Aktoren, deren *STATE-Feld Blockier-/Fehlgründe trägt — die
// Diagnose-Annotation hängt sie an den Rohwert an (nur Anzeige, kein Alarm:
// SOLARSTATE steht auf der Referenz dauerhaft auf BLOCKED_BY_SENSOR_FAULT).
const DIAG_STATE = /** @type {Object<string, string>} */ ({
  pump_running: 'PUMPSTATE',
  heater_active: 'HEATERSTATE',
  solar_active: 'SOLARSTATE',
});
```

In `diagRawValue` das Ende der Funktion (ab `if (val === undefined || val === null) return null;`) ersetzen durch:

```js
  if (val === undefined || val === null) return null;
  let out = Array.isArray(val) ? `[${val.join(', ')}]` : String(val);
  const stateField = DIAG_STATE[base];
  if (stateField) {
    const reasons = stateReasons(raw[stateField]);
    if (reasons.length) out += ` | ${reasons.join(', ')}`;
  }
  return out;
```

- [ ] **Step 4: Pass verifizieren** — Run: `npm test` → fail 0.

- [ ] **Step 5: Commit**

```bash
git add lib/FeatureGroups.js test/FeatureGroups.test.js
git commit -m "feat(m5.8): Diagnose-Annotation zeigt STATE-Blockiergruende an Pumpe/Heizung/Solar"
```

---

### Task 7: Gates, Doku, Release-Vorbereitung 0.6.0

**Files:**
- Modify: `docs/dashboard/dashboard.html` (nur M5.8-Objekt: Steps, Log, currentActivity)
- Modify: `.homeychangelog.json`, `docs/dashboard/versions.md` (beim Release)
- Verify: gesamte Suite + validate publish + release-readiness

- [ ] **Step 1: Volle Gates fahren** — Run: `npm test` (Erwartung: fail 0, **todo 0**) und `npx homey app validate --level publish` (Erwartung: PASS). Output im Chat zeigen (DONE-Bedingungen 1+2).

- [ ] **Step 2: Live-Smoke vorbereiten (kein Release):** Kein Schritt nötig — Install erst nach Release-Gates.

- [ ] **Step 3: Version bumpen** — Run: `npx homey app version minor` (0.5.1 → 0.6.0, Milestone-Start per HOMEY.md). `.homeychangelog.json` um `0.6.0` ergänzen (en+de, ein Satz: Messeingangs-Kacheln aus der Violet-Config + Reichweiten-Parser-Fix). Danach `npx homey app validate --level publish` erneut (regeneriert app.json; Versionen müssen identisch sein).

- [ ] **Step 4: Dashboard + versions.md** — M5.8-Steps „Implementierung (SDD)" done; Log-Eintrag mit Testzahlen; versions.md-Zeile für 0.6.0 (Ziel: Homey-Install) VOR dem Install eintragen.

- [ ] **Step 5: Commit**

```bash
git add .homeycompose/app.json app.json .homeychangelog.json docs/dashboard/versions.md docs/dashboard/dashboard.html
git commit -m "chore(release): 0.6.0 — M5.8 Messeingaenge + Parser-Fixes (Changelog en+de)"
```

- [ ] **Step 6: release-readiness-Subagent** laufen lassen (Erwartung: PASS — DONE-Bedingung 3), dann `npx homey app install` auf „Torstens Homey Pro"; Live-Smoke: Kacheln „Filterdruck" (≈0.3 Bar bei laufender Pumpe) und „Anströmung" (>0 cm/s) sichtbar, CL-Reichweite zeigt wieder einen Wert (~1.4 d statt stale).

- [ ] **Step 7: Abschluss** — Dashboard M5.8 `status: "done"` + commit-SHA; danach CLAUDE.md §9 (Code-Review + Merge-Frage an den User).

---

## Self-Review (erledigt)

- **Spec-Abdeckung:** §3→Tasks 4+5, §4→Task 3, §5→Tasks 1+2, §6→Task 6, §7→Task 4 Tests/§3-Gating, §8→Testschritte in jedem Task, §9→keine neuen Deps/Whitelist-Check in Task 3, §10→Task 7. Non-Goals: keine Alarm-Tasks enthalten. ✓
- **Platzhalter:** keine (jeder Code-Schritt trägt vollständigen Code). Einzige bewusste Anpassungsstelle: Mock-Property-Namen in Task 5 Step 3 (Muster in Bestandstests derselben Datei). ✓
- **Typ-Konsistenz:** `stateReasons`/`stateBlocked` (Task 2) = Verwendung in Task 6; `INPUT_SUBCAPS` (Task 4) = Import in Task 5; ConfigFacts-Shape (Task 3) = Options-Block (Task 5). ✓
