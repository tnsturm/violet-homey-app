# M4.8 Loop-Hardening III: Autonomie & Meta-Loop — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Hook-Telemetrie (hook-log.jsonl) + nächtliche lokale Triage-Routine mit committeter Inbox + milestone-checkpoint-Schritte Memory-Konsolidierung/Handover — Spec: `docs/superpowers/specs/2026-07-09-m4.8-loop-hardening-autonomy-metaloop.md`.

**Architecture:** Telemetrie als fail-silenter Helper (`.claude/hooks/lib/log.js`), cwd-basierte Pfadauflösung hält Fixture-Läufe sauber (Spec §3 D2); Einzeiler-Instrumentierung an den Entscheidungspunkten aller 9 Hooks (D3). Triage = lokaler Scheduled Task (B1: Cloud kann weder MEMORY.md lesen noch lokal committen) mit selbstständigem Prompt. Skill-Erweiterungen sind reine Text-Edits mit klaren Schutzregeln (Diff-Review, nie löschen).

**Tech Stack:** Node (`node:test`), scheduled-tasks-MCP (lokal), Markdown-Skill-Edits.

## Global Constraints

- Tooling-only: KEIN Version-Bump, kein Release (§8); `dependencies` bleibt `{}`.
- Telemetrie darf NIE einen Hook brechen (try/catch fail-silent, D1) und NIE aus Fixture-Läufen ins echte Log schreiben (cwd-Auflösung, D2).
- `pass` wird NUR auf dem geprüft-und-gut-Pfad geloggt, nicht auf fail-open-/nicht-zuständig-Exits (D3 — sonst Rauschen).
- Memory-Konsolidierung: Ergebnis IMMER als Diff, NIE direkt anwenden; offene Follow-ups/Security-Notizen NIE löschen (D13).
- Branch `m4.8-loop-hardening-autonomy` von `main`; Push nur nach /code-review + explizitem Ja (§9).
- Hooks sind LIVE (B2): Hook-Edits konservativ (fail-open bleibt), Suite nach jedem Hook-Edit grün bevor committet wird.
- documenting-code für neue Dateien (log.js/spawn-env.js sind pure Helper → Header + JSDoc auf Exports).

---

### Task 0: Branch + Basis-Commit

- [ ] **Step 0.1:** `git checkout -b m4.8-loop-hardening-autonomy`
- [ ] **Step 0.2:**

```bash
git add docs/superpowers/specs/2026-07-09-m4.8-loop-hardening-autonomy-metaloop.md docs/superpowers/plans/2026-07-09-m4.8-loop-hardening-autonomy-metaloop.md docs/dashboard/dashboard.html
git commit -m "docs(m4.8): Kurz-Spec + Plan, Dashboard M4.8 active (B1 scheduled-tasks lokal = richtige Wahl, Telemetrie-Design cwd-basiert)"
```

---

### Task 1: Hook-Telemetrie + Env-Strip-Dedupe (TDD)

**Files:**
- Create: `.claude/hooks/lib/log.js`, `.claude/hooks/lib/spawn-env.js`
- Create: `test/hooks/hook-log.test.js`
- Modify: `.gitignore` (+1 Zeile), alle 9 Hooks in `.claude/hooks/*.js`

**Interfaces:**
- Produces: `logHook(hook: string, decision: 'block'|'pass', cwd?: string): void` (fail-silent) und `spawnEnv(): object` (process.env ohne `NODE_TEST_CONTEXT`/`NODE_TEST_WORKER_ID`).

- [ ] **Step 1.1: Failing Test** — `test/hooks/hook-log.test.js`:

```js
'use strict';

// Smoke test for .claude/hooks/lib/log.js — the telemetry helper appends one
// parseable JSONL decision record to <cwd>/.claude/hooks/hook-log.jsonl and is
// strictly fail-silent: no throw, no file when the target dir doesn't exist
// (that is what keeps fixture-spawned hook runs from polluting anything) —
// M4.8 spec §3 D1/D2 (docs/superpowers/specs/2026-07-09-m4.8-loop-hardening-autonomy-metaloop.md).

const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');

const { logHook } = require('../../.claude/hooks/lib/log');

test('logHook: appends a parseable JSONL line when .claude/hooks exists', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'hook-log-'));
  fs.mkdirSync(path.join(dir, '.claude', 'hooks'), { recursive: true });
  logHook('test-gate', 'block', dir);
  logHook('test-gate', 'pass', dir);
  const lines = fs.readFileSync(path.join(dir, '.claude', 'hooks', 'hook-log.jsonl'), 'utf8')
    .trim().split('\n').map((l) => JSON.parse(l));
  assert.strictEqual(lines.length, 2);
  assert.strictEqual(lines[0].hook, 'test-gate');
  assert.strictEqual(lines[0].decision, 'block');
  assert.strictEqual(lines[1].decision, 'pass');
  assert.match(lines[0].ts, /^\d{4}-\d{2}-\d{2}T/);
});

test('logHook: silent no-op when the target dir does not exist (fixture safety)', () => {
  const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'hook-log-'));
  assert.doesNotThrow(() => logHook('test-gate', 'block', dir));
  assert.strictEqual(fs.existsSync(path.join(dir, '.claude')), false);
});

test('spawnEnv: strips the node:test child markers, keeps the rest', () => {
  const { spawnEnv } = require('../../.claude/hooks/lib/spawn-env');
  process.env.NODE_TEST_CONTEXT = 'child-v8';
  const env = spawnEnv();
  delete process.env.NODE_TEST_CONTEXT;
  assert.strictEqual(env.NODE_TEST_CONTEXT, undefined);
  assert.strictEqual(env.NODE_TEST_WORKER_ID, undefined);
  assert.strictEqual(typeof env.PATH === 'string' || typeof env.Path === 'string', true);
});
```

- [ ] **Step 1.2: RED** — `node --test test/hooks/hook-log.test.js` → FAIL (Module fehlen).
- [ ] **Step 1.3: Helper implementieren** — `.claude/hooks/lib/log.js`:

```js
'use strict';

// Hook telemetry (pure helper) — M4.8 spec §3 D1/D2
// (docs/superpowers/specs/2026-07-09-m4.8-loop-hardening-autonomy-metaloop.md).
// One JSONL decision record per hook decision, appended to the guarded repo's
// .claude/hooks/hook-log.jsonl (gitignored). Resolution is cwd-based on purpose:
// hook smoke tests spawn hooks with fixture cwds that have no .claude/hooks/
// dir, so those runs skip silently instead of polluting the real log (or the
// fixture). Strictly fail-silent — telemetry must never break a hook.

const fs = require('fs');
const path = require('path');

/**
 * Append one decision record ({ts, hook, decision}) to the repo-local hook log.
 * Never throws (spec D1); no-op when <cwd>/.claude/hooks/ does not exist (D2).
 * @param {string} hook Hook name, e.g. `test-gate`.
 * @param {'block'|'pass'} decision Outcome at a real decision point (D3).
 * @param {string} [cwd] Guarded repo root (the hook-input cwd); defaults to process.cwd().
 */
function logHook(hook, decision, cwd) {
  try {
    fs.appendFileSync(
      path.join(cwd || process.cwd(), '.claude', 'hooks', 'hook-log.jsonl'),
      `${JSON.stringify({ ts: new Date().toISOString(), hook, decision })}\n`
    );
  } catch {
    // telemetry must never break a hook (spec D1)
  }
}

module.exports = { logHook };
```

`.claude/hooks/lib/spawn-env.js`:

```js
'use strict';

// Spawn env for nested node:test runs (pure helper) — M4.8 spec §3 D7, dedupe of
// the M4.6/M4.7 lesson (docs/superpowers/specs/2026-07-09-m4.8-loop-hardening-autonomy-metaloop.md):
// inherited NODE_TEST_CONTEXT/NODE_TEST_WORKER_ID flip a child `node --test`
// into the runner's child protocol — exit 0 despite failures, no readable TAP.

/**
 * process.env copy without the node:test child-process markers (spec D7).
 * @returns {Object<string, string|undefined>} Env safe for spawning nested suites.
 */
function spawnEnv() {
  const env = { ...process.env };
  delete env.NODE_TEST_CONTEXT;
  delete env.NODE_TEST_WORKER_ID;
  return env;
}

module.exports = { spawnEnv };
```

- [ ] **Step 1.4: GREEN** — `node --test test/hooks/hook-log.test.js` → 3 pass.
- [ ] **Step 1.5: .gitignore** — unter `# Node` ergänzen:

```
# Hook telemetry (local only — M4.8)
.claude/hooks/hook-log.jsonl
```

- [ ] **Step 1.6: Instrumentierung** — je Hook `const { logHook } = require('./lib/log');` nach den bestehenden requires; dann Einzeiler nach D3-Regel (block vor jedem `exit 2`; pass NUR am geprüft-gut-Exit). Platzierung je Hook (Datei vorher lesen, Zeilen konservativ einsetzen):
  - `check-version-sync.js`: block vor dem Mismatch-`exit 2`; pass vor dem finalen `exit 0` (nach bestandenem Vergleich).
  - `typecheck-gate.js` / `test-gate.js`: den kombinierten `if (r.status === 0 || r.status === null)`-Exit aufteilen — `r.status === 0` → pass+exit, `r.status === null` → fail-open-exit OHNE Log; block vor dem `exit 2`. In `test-gate.js` zusätzlich den Env-Block durch `const env = spawnEnv();` ersetzen (`require('./lib/spawn-env')`).
  - `release-gate.js`: block vor `exit 2`; pass vor dem finalen `exit 0` (alle Bedingungen geprüft).
  - `secrets-guard.js` / `compose-guard.js` / `json-guard.js`: block vor jedem Befund-`exit 2`; pass nur am Ende des geprüft-sauber-Pfads (frühe nicht-zuständig-Exits loggen nicht).
  - `run-matching-test.js`: nach dem `spawnSync(..., '--test', ...)` eine pass-Zeile (Hook hat den gemappten Test ausgeführt); Env-Block durch `spawnEnv()` ersetzen.
  - `stop-verify.js`: block vor `exit 2`; pass am „guarded dirty, aber alles grün"-Exit (NICHT an den Kurzschluss-Exits); Env-Block durch `spawnEnv()` ersetzen.
- [ ] **Step 1.7: Suite** — `npm test` → fail 0, todo 1 (bestehende Hook-Tests beweisen D2 nebenbei: Fixture-cwds erzeugen kein Log).
- [ ] **Step 1.8: E2E-Verify (Prompt-Auflage)** — absichtlich kaputten JSON-Edit an `.homeychangelog.json` machen (live json-guard meldet), dann:

```powershell
Get-Content .claude/hooks/hook-log.jsonl | Select-Object -Last 3
```

Expected: mindestens eine Zeile `"hook":"json-guard","decision":"block"` — im Chat zeigen; Edit sofort rückgängig machen (`git checkout -- .homeychangelog.json`).

- [ ] **Step 1.9: Commit**

```bash
git add .claude/hooks/ test/hooks/hook-log.test.js .gitignore
git commit -m "feat(hooks): Telemetrie hook-log.jsonl (fail-silent, cwd-basiert) in allen 9 Hooks + spawn-env-Dedupe (M4.8 Idee 11)"
```

---

### Task 2: Triage — manueller Lauf, Inbox, Scheduled Task, §7-Regel

**Files:**
- Create: `docs/dashboard/triage-inbox.md`
- Modify: `CLAUDE.md` (§7 Rules, +1 Bullet), `.claude/agents/release-readiness.md` (+Hinweis)
- Extern: Scheduled Task `violet-nightly-triage` (`~/.claude/scheduled-tasks/`)

- [ ] **Step 2.1: Manueller Triage-Lauf** (D9-Schritte in dieser Session): `npm test` · `npx homey app validate --level publish` · Versions-Sync-Einzeiler · `gh run list --workflow=ci.yml --limit 1` · offene Follow-ups aus MEMORY.md/project-status abgleichen (bekannt: Write-Passwort-Rotation vor Live-Publish; homeyCommunityTopicId; App-Crash-Trace nicht reproduzierbar; alarm_dosing_blocked-Defekt = todo-Test; M5-Gate c strict-Ratchet; Branch claude/write-path-security-reviewer-Entscheid bei M6).
- [ ] **Step 2.2: Inbox schreiben** — `docs/dashboard/triage-inbox.md` (Format D10):

```markdown
# Triage-Inbox

Nächtliche Befund-Sammlung der `violet-nightly-triage`-Routine (M4.8, Spec §4) —
**jede neue Milestone-Session und der release-readiness-Subagent lesen diese Datei zuerst**
(CLAUDE.md §7). Die Routine pflegt die Abschnitte (Einträge wandern), sie hängt nicht endlos an.

**Stand:** 2026-07-09 (manueller Erstlauf, M4.8-Session)

## Offen

- 2026-07-09 · Follow-up · Write-Passwort-Rotation vor Live-Publish (release-gate blockt publish bis docs/superpowers/security/credential-rotation.md existiert) — Memory security-rotate-violet-credential.
- 2026-07-09 · Follow-up · homeyCommunityTopicId in .homeycompose/app.json füllen, sobald die Forum-Launch-Posts live sind (versions.md).
- 2026-07-09 · Follow-up · App-Crash-Trace (M3) nicht reproduzierbar — braucht User-Trace oder Store-Test-Crash-Reports; beobachten.
- 2026-07-09 · Test/todo · alarm_dosing_blocked-False-Positive (M2-Logik) — als todo-Test eingefroren (test/FeatureGroups.test.js), Fix ausstehend.
- 2026-07-09 · Follow-up · M5-Gate (c): checkJs-strict-Ratchet ausreizen (Gates a+b sind erfüllt).
- 2026-07-09 · Follow-up · Branch claude/write-path-security-reviewer: merge-vs-drop bei M6 entscheiden.

## Neu (2026-07-09)

- 2026-07-09 · Lauf · npm test: <BEFUND AUS 2.1> · validate publish: <BEFUND> · Versions-Sync: <BEFUND> · CI-Lauf: <BEFUND>.

## Erledigt (2026-07-09)

- (noch keine — Erstlauf)
```

(Platzhalter `<BEFUND …>` mit den ECHTEN Ergebnissen aus Step 2.1 füllen — grüne Läufe als eine Sammelzeile.)

- [ ] **Step 2.3: §7-Regel** — CLAUDE.md, im §7-**Rules**-Block als neuen Bullet nach der FRICTION-Regel:

```
- New milestone sessions and the release-readiness subagent read `docs/dashboard/triage-inbox.md`
  FIRST (when present) — surface open findings before starting new work.
```

`release-readiness.md`, nach der Einleitung („Nimm keine Änderungen vor…"): `Lies zuerst docs/dashboard/triage-inbox.md (falls vorhanden) und erwähne offene Findings im Bericht.`

- [ ] **Step 2.4: Commit**

```bash
git add docs/dashboard/triage-inbox.md CLAUDE.md .claude/agents/release-readiness.md
git commit -m "feat(triage): Inbox-Erstlauf + CLAUDE.md-§7-Regel (Inbox zuerst lesen) + release-readiness-Hinweis (M4.8 Idee 8)"
```

- [ ] **Step 2.5: Scheduled Task anlegen** — `mcp__scheduled-tasks__create_scheduled_task` mit `taskId: violet-nightly-triage`, `cronExpression: "30 3 * * *"`, selbstständigem Prompt (D9): Repo-Pfad C:/Users/TorstenSturm/source/repos/VioletApp; Schritte npm test → npx homey app validate --level publish → Versions-Sync-Einzeiler → gh run list (letzter CI-Lauf) → Follow-up-Abgleich gegen MEMORY.md/triage-inbox; docs/dashboard/triage-inbox.md nach Format pflegen (Offen/Neu/Erledigt, Datum je Eintrag, Stand-Zeile bumpen); NUR diese Datei committen (`chore(triage): nightly <Datum>`), NICHT pushen; read-only sonst, keine Fixes. Nachweis: `list_scheduled_tasks` im Chat.

---

### Task 3: milestone-checkpoint- + dashboard-sync-Skill erweitern

**Files:**
- Modify: `.claude/skills/milestone-checkpoint/SKILL.md`
- Modify: `.claude/skills/dashboard-sync/SKILL.md` (Checkpoint-Steps-Beschreibung)
- Modify: `docs/dashboard/dashboard.html` (NUR `→M5.steps[]`: zwei neue Labels)

- [ ] **Step 3.1: Schrittliste ersetzen** (milestone-checkpoint/SKILL.md, `## Schritte`):

```markdown
1. `/fewer-permission-prompts` ausführen.
2. `/claude-automation-recommender` ausführen.
3. Skill-Quellen prüfen (siehe unten).
4. **Workflow-Retrospektive / Optimizer** ausführen (siehe unten) — wiederkehrende Reibung aus dem
   abgeschlossenen Milestone in eine dauerhafte Absicherung überführen.
5. **Memory-Konsolidierung** (siehe unten) — Memory-Dateien eindampfen, Ergebnis nur als Diff.
6. Den aktiven `→Mx`-Checkpoint-Eintrag in `docs/dashboard/dashboard.html` aktualisieren:
   `status: "done"`, `finishedAt` = heute, alle Steps abgehakt, je ein `log[]`-Eintrag mit
   kurzer Zusammenfassung der Schritte 1–5.
7. **Handover** (siehe unten) — den nächsten Milestone als Push aufs Handy übergeben.
```

- [ ] **Step 3.2: Retro-Signalquelle** (Schritt-4-Punkt 1 ergänzen): nach „und der Git-Verlauf …)" anfügen: ` Zusätzlich .claude/hooks/hook-log.jsonl auslesen (Block-Zählungen je Hook seit dem letzten Checkpoint statt Erinnerung — M4.8).`
- [ ] **Step 3.3: Neue Sektion nach „## Schritt 4"-Block:**

```markdown
## Schritt 5: Memory-Konsolidierung (Dreaming-Muster, M4.8)

Sessions seit dem letzten Checkpoint sichten (`search_session_transcripts`, falls verfügbar —
sonst Dashboard-`log[]` und `git log` als Quellen), dann die Dateien im Memory-Ordner
(`MEMORY.md` + Einzeldateien) konsolidieren:

- **Deduplizieren/kürzen**: Erledigtes eindampfen (z. B. trägt `project-status` volle
  Detailhistorien abgeschlossener Milestones, die eine Zeile + Verweis sein können).
- **Widersprüche auflösen**: veraltete Aussagen korrigieren (z. B. "ab nächster Session aktiv",
  wenn es längst live ist).
- **HARTE REGELN**: das Ergebnis IMMER als Diff zum Review präsentieren, NIE direkt anwenden;
  offene Follow-ups und Security-Notizen NIE löschen; im Zweifel behalten.
```

- [ ] **Step 3.4: Neue Sektion vor „## Bericht":**

```markdown
## Schritt 7: Handover (M4.8)

1. Aus dem `DASHBOARD_STATUS`-Block den NÄCHSTEN Milestone mit `status: "todo"` (erster in
   Listenreihenfolge) lesen.
2. Push-Benachrichtigung aufs Handy senden (PushNotification): Titel `Nächster Milestone: <id>
   — <title>`, Text = Kurzfassung (erste Prompt-Zeilen) + Hinweis `Start per /remote-control
   <id>` (der volle Prompt steht im Dashboard; Push hat Längenlimits). Kein Push-Kanal
   verfügbar → Prompt-Kopf im Chat zeigen und das im Log vermerken.
3. Fragen, ob der Milestone direkt in dieser Session gestartet werden soll.
```

- [ ] **Step 3.5: dashboard-sync/SKILL.md** — im Abschnitt „## Checkpoint-Einträge (`→Mx`)" den Satz „Vier Steps, alle über den `milestone-checkpoint`-Skill abgedeckt: …" ersetzen durch:

```
Sechs Steps, alle über den `milestone-checkpoint`-Skill abgedeckt: `/fewer-permission-prompts`,
`/claude-automation-recommender`, `Skill-Quellen geprüft`, `Workflow-Retrospektive`,
`Memory-Konsolidierung`, `Handover` (M4.8).
```

- [ ] **Step 3.6: →M5-Steps** (dashboard.html, NUR das `→M5`-Objekt): nach `{ label: "Workflow-Retrospektive", done: false }` zwei Labels ergänzen: `{ label: "Memory-Konsolidierung", done: false }` und `{ label: "Handover", done: false }`. Danach node-Eval-Parse-Check. (→M6–→M8 absichtlich nicht — das ist der M4.9-Konsistenz-Sweep.)
- [ ] **Step 3.7: Commit**

```bash
git add .claude/skills/milestone-checkpoint/SKILL.md .claude/skills/dashboard-sync/SKILL.md docs/dashboard/dashboard.html
git commit -m "feat(checkpoint): Schritte Memory-Konsolidierung + Handover, Retro liest hook-log.jsonl; dashboard-sync-Muster + ->M5-Steps (M4.8 Ideen 9+10)"
```

---

### Task 4: Abnahme (Spec §6) + Dashboard + /code-review + §9

- [ ] **Step 4.1:** `npm test` → fail 0, todo 1 (Chat). `hook-log.jsonl`-block-Zeile (aus 1.8) im Chat. Triage-Inbox-Commit + `list_scheduled_tasks`-Nachweis im Chat. SKILL.md-Diff (Handover/Konsolidierung) im Chat.
- [ ] **Step 4.2:** Dashboard M4.8: Steps Spec/Plan/Implementierung done, Logs (D-Entscheidungen, Befunde), currentActivity Review/Push; Commit.
- [ ] **Step 4.3:** `/code-review` Branch-Diff; Funde fixen; §9-Frage (direkt push vs. PR) — auf explizites Ja warten. Nach Push: CI grün verifizieren, Dashboard done (prompt: null), Memory aktualisieren.
