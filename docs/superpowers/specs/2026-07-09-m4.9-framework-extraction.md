# M4.9 — Framework-Extraktion: agentic-loop-framework (Kurz-Spec)

**Datum:** 2026-07-09 · **Milestone:** M4.9 · **Basis:** Bootstrap-Prompt Phase 6
(`skill-ClaudeCode-general-settings/prompts/agentic-loop-bootstrap.de.md`), Memory
`loop-dev-roadmap`, Resume-Prompt. Tooling-only: kein Version-Bump, kein Release (§8).
Kein Threat-Model (§5: reine Doku-/Template-Extraktion, keine neue Angriffsfläche).
**User-Entscheid (2026-07-09): Zielort = eigenes privates Repo `skill-agentic-loop-framework`**
(sauberste Trennung, eigener Marketplace-Eintrag, später unabhängig teilbar).

## §1 Scope

1. Neues Repo `skill-agentic-loop-framework` (privat, via gh) mit Plugin-Struktur nach dem
   Muster des Settings-Repos: `.claude-plugin/marketplace.json` + `plugin/.claude-plugin/
   plugin.json` + `plugin/skills/agentic-loop-framework/{SKILL.md, templates/, homey/,
   CHANGELOG.md}`.
2. SKILL.md = generalisierte Bootstrap-Anleitung (Phasen 0–7 + Dauerregeln) aus Prompt A;
   Homey-Zusatz (Prompt B) wandert nach `homey/README.md`.
3. `templates/` = generische Vorlagen als **kopierfähiger Projektbaum**; `homey/` = die
   Plattform-Bausteine. Jede Vorlage frei von VioletApp-/Homey- bzw. VioletApp-Bezügen.
4. VioletApp-Repo: Spiegelregel-Erweiterung (CLAUDE.md) + Drift-Check-Schritt
   (milestone-checkpoint) + Prompt-Konsistenz-Sweep (→M5, →M6–→M8, M5–M10).
5. Trockentest + /code-review in BEIDEN Repos vor Git-Aktion (§9).

## §2 Entscheidungen

- **D1 — Repo/Struktur:** `gh repo create tnsturm/skill-agentic-loop-framework --private`;
  lokal unter `C:/Users/TorstenSturm/source/repos/skill-agentic-loop-framework`. Plugin-
  Layout wie oben (marketplace-installierbar wie das Settings-Repo).
- **D2 — Keimzellen-Prompts:** Die Bootstrap-Prompts BLEIBEN im Settings-Repo (historischer
  Seed, §3 surgical — nichts löschen); sie bekommen je eine 1-Zeilen-Superseded-Notiz +
  README-Verweis auf das neue Repo. SKILL.md wird die lebende Quelle (de; die en-Variante
  bleibt referenziert als `prompts/agentic-loop-bootstrap.en.md` im Settings-Repo).
- **D3 — templates/ (generisch), als Baum in Ziel-Layout** (Trockentest kopiert 1:1):
  - `CLAUDE.md` — VioletApp-Stand §0–§11 generalisiert (inkl. Karpathy-Kern §1–§4,
    todo-Test-Regel §4, §5 Security, §6 <PLATFORM>.md-Mechanismus, §7 Dashboard-Protokoll
    inkl. FRICTION- + Triage-Inbox-Regel, §8 Versionierung, §9 Branch-Gate, §10
    Permission-Strategie, §11 Subagent-Tiering); Projekt-Extensions raus, Platzhalter rein.
  - `docs/dashboard/dashboard.html` — Datenblock LEER (`milestones: []`), Renderer 1:1
    aus VioletApp (nie editieren); `docs/dashboard/versions.md` — Tabellen-Skelett;
    `docs/dashboard/triage-inbox.md` — Format-Skelett (M4.8).
  - `.claude/settings.json` — leeres hooks-Gerüst (PreToolUse Bash|PowerShell / Edit|Write,
    PostToolUse, Stop) + minimale read-only-Allowlist (git fetch/merge-base) — plattformfrei.
  - `.claude/hooks/` — `test-gate.js` (bereits generisch: liest `package.json scripts.test`;
    Kopf-Kommentar dokumentiert die Anpassungsstelle für Nicht-npm-Stacks),
    `lib/log.js` + `lib/spawn-env.js` (M4.8-Helper, generisch), dazu
    `test/hooks/test-gate.test.js` als Smoke-Test-Muster (Pfad-Layout passt im Baum).
  - `.claude/skills/milestone-checkpoint/SKILL.md` — generalisiert (Skill-Quellen-Abschnitt
    → Platzhalter; inkl. M4.8-Schritte Memory-Konsolidierung + Handover + Telemetrie-Zeile
    UND des NEUEN Drift-Check-Schritts aus D6).
- **D4 — homey/**: `README.md` (= Prompt-B-Inhalt generalisiert, Einbau-Anleitung),
  `HOMEY.md` (Template, App-neutral), Hook-Vorlagen `compose-guard.js`, `json-guard.js`,
  `check-version-sync.js`, `secrets-guard.js` (Violet-Wortlaut → generisch
  „device write credential"; Struktur/Telemetrie-Zeilen bleiben), `allowlist.json`
  (Homey-read-only-Block), `release-readiness.md` (App-Name-neutral, Punkte 1–8;
  Live-Smoke-Punkt als „projektspezifisches Skript" markiert).
- **D5 — CHANGELOG.md:** `0.1.0` (2026-07-09) — initiale Extraktion aus VioletApp
  M0–M4.8; Quelle je Template kurz genannt.
- **D6 — VioletApp-Änderungen:** (a) CLAUDE.md-Abschnitt „Skill = Source of Truth" um die
  Framework-Spiegelregel erweitern: jede GENERISCHE Änderung an Hooks/Protokollregeln/
  Checkpoint-Ablauf/Templates wird in derselben Session in `skill-agentic-loop-framework`
  gespiegelt (Faustregel bleibt: überall sinnvoll → Skill; nur hier → Projekt).
  (b) milestone-checkpoint-Skill: neuer **Schritt 6 „Framework-Drift prüfen"** (unterscheiden
  sich Projekt-Framework und Skill? → Skill nachziehen; Quelle: git log der Framework-Dateien
  seit letztem Checkpoint), Dashboard-Update wird 7, Handover 8; dashboard-sync-Muster +
  →M5-Steps auf 7 Labels (→M6–→M8 im Sweep D7).
- **D7 — Sweep** über die offenen Prompts (→M5, →M6, →M7, →M8, M5, M6, M7, M8, M9, M10),
  chirurgisch nur im jeweiligen `prompt`-Feld (+ Checkpoint-`steps[]`):
  - (a) Done-Bedingung: M5–M10 haben DONE-BEDINGUNGEN (M4.6) — prüfen; →Mx-Checkpoint-
    Prompts bekommen eine kurze Checkliste (Steps 1–8 durchlaufen + →Mx-Eintrag done).
  - (b) §10-Startmodus-Zeile in jeden Prompt: „Startmodus (CLAUDE.md §10): autonomer Lauf
    → claude --permission-mode auto; Alltags-Session → Default-Modus (Allowlist greift)."
  - (c) Triage-Inbox-Zeile (M4.8) in jeden Milestone-/Checkpoint-Prompt: „Lies zuerst
    docs/dashboard/triage-inbox.md (§7)".
  - (d) Veraltete Referenzen: M5-Gate-Formulierungen („wird in M4.6/M4.7 gebaut" → „seit
    M4.6/M4.7 vorhanden; offen nur (c) strict-Ratchet"); →Mx-`steps[]` von 4 auf 7 Labels;
    keine sonstigen toten Pfade (je Prompt prüfen).
  - Danach Datenblock-node-Eval + Diff im Chat.
- **D8 — Trockentest (Phase-6-Verify):** `templates/`-Baum in Temp kopieren →
  dashboard.html-Datenblock parst (node-Eval, leere milestones ok), settings.json parst
  (JSON.parse), `node --test test/hooks/test-gate.test.js` im Temp-Baum grün (Hook +
  Smoke-Test funktionieren im ausgelieferten Layout).

## §3 Verifikation (Abnahme, = /goal)

1. Trockentest: Datenblock parst · settings.json parst · Hook-Smoke-Tests grün — im Chat.
2. SKILL.md + templates/ + homey/ + CHANGELOG committed im neuen Repo (git log gezeigt).
3. VioletApp: CLAUDE.md-Spiegelregel erweitert + milestone-checkpoint-Drift-Check vorhanden.
4. Sweep über →M5/→M6–→M8/M5–M10 durchgeführt: Diffs gezeigt, Datenblock-Eval grün.
5. /code-review in BEIDEN Repos vor jeder Git-Aktion; §9-Freigaben abwarten.
