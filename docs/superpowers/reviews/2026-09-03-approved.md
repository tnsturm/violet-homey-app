---
review: adversarial-reviewer + /code-review medium über den Fix-Diff d3f4372 (§9 Schritt 3)
head: ea41c4e7259ab257a5df03ef4215dc0ceb93f334
branch: fix/repair-view-folder (PR #20)
date: 2026-09-03
triage: erledigt (Nutzer, 2026-09-03)
---

# Nachreview des Repair-View-Fixes — Triage

Kontext: Live-Fund 2026-09-03 (`unknown_error_getting_file` beim Repair-Dialog), Fix d3f4372
verschiebt `drivers/pool/pair/repair.html` nach `drivers/pool/repair/` und pinnt die
Ordnerkonvention per Test. `/code-review medium`: 0 Funde. `adversarial-reviewer`: 2 Funde,
beide am neuen Test, beide mit Repro (Scratchpad-Exporte, Repo unberührt).

| # | Fund | Entscheidung | Begründung |
|---|---|---|---|
| F1 | Test iteriert `compose.repair \|\| []` — ohne `repair`-Block im Manifest schweigt er; Repair-Dialog kann aus dem Produkt verschwinden, validate + Suite bleiben grün (Repro: Block gelöscht → `app.json.repair` undefined, 462/462 grün) | **approved** | Der Test ist der einzige Wächter dieser Klasse, validate deckt View-Verdrahtung nicht ab. |
| F2 | Test liest `driver.compose.json`, die CLI überschreibt `pair`/`repair` aber komplett, sobald `driver.repair.compose.json` existiert (Zuweisung, kein Merge) — Repro: View dort als `repair_creds`, Datei bleibt `repair.html`, validate PASS, Suite grün, Runtime würde erneut die 0.9.0-Fehlerklasse liefern | **approved** | Maßgeblich ist das generierte `app.json`, das Homey liest und das per check-version-sync synchron gehalten wird. |

Gemeinsamer Fix (test-first): Erwartung gegen `app.json` (Pool-Treiber) statt gegen die
Compose-Datei; zusätzlich positiv pinnen, dass ein template-loser Repair-View mit Id `repair`
existiert. RED wird gegen eine mutierte Manifest-Kopie im Scratchpad gezeigt (F1-Szenario),
nicht im Repo.

Angegriffen, hält: Revert nach `pair/` macht den Test rot; keine Restpfade im Code; Bundle
trägt `repair/repair.html`; committetes `app.json` identisch zum regenerierten;
Case-Insensitivität von `existsSync` auf Windows wird von der Ubuntu-CI abgefangen.

Zur Kenntnis (kein Fund): R2-3 in der Triage-Inbox setzt „Assets aus dem pair/-Ordner"
voraus — gilt seit dem Move nur noch für `connect.html`.
