# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Hook telemetry (local only — M4.8)
.claude/hooks/hook-log.jsonl

# Homey
.homeybuild/
.homeycompose/app.json.tmp
env.json
*.homeycompose.log

# Secrets / credentials (never commit Violet write credentials)
.env
*.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Marketing/launch drafts — scratch, not versioned (deleted after posting)
docs/marketing/
